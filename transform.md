# 手続型→関数型変換への3論文からの抽出（CPS/SSA対応・相互変換・CPS変換の定式化）

出典:
- [K] Richard A. Kelsey, "A Correspondence between Continuation Passing Style and Static Single Assignment Form", ACM IR'95, 1995.
- [M] 森下大輔, "手続き型プログラムと関数型プログラムの相互変換手法", 情報処理学会第75回全国大会, 2013.
- [S] 住井英二郎・大根田裕一・米澤明憲, "例外処理機構を備えた命令型言語のCPS変換とその定式化", 情報処理学会論文誌, 2004.

本ドキュメントは、上記3論文から本プロジェクト（Blockly手続型AST → 関数型AST → call-by-need純粋評価器 → トレース駆動グラフ描画）の**②変換ステージ（transpiler.ts）および§5.1/§5.2の将来実装**に流用できる技術を抽出したものである。

---

## 0. 適合性の前提（最初に読むこと）

3論文と本システムの決定的な差異は2点ある。**流用の際は必ずこの差異を通すこと。**

1. **評価戦略**: 3論文はすべて **call-by-value（正格評価）** を前提とする。本システムは **call-by-need（指示書§3）**。変換の「構造」（ブロック→関数、goto→末尾呼び出し、φ→引数）は評価戦略に依存せず流用可能だが、「意味」の証明と、アキュムレータの扱い（本書§5）には評価戦略依存の注意が必要。
2. **変換スタイル**: [K][S]はCPSを、[M]は末尾呼び出し形式の直接スタイル（OCamlサブセット）をターゲットとする。本システムは direct style + ネストしたlet（指示書§2）を採用済みであり、**[M]のターゲット形式が本システムに最も近い**。[K][S]のCPS固有部分（継続k・例外ハンドラh）は現行スコープでは不採用とし、非局所制御（break/早期return/例外）導入時のロードマップとして保管する（本書§6）。

各項目の末尾に `[流用: 直接 / §5.x実装時 / 将来 / 検証用]` を付す。

---

## 1. 【核心】基本ブロック→関数、ループ→末尾再帰関数の変換スキーム

### 1-1. 基本ブロック = 関数、ジャンプ = 末尾呼び出し（[S] §2.1, [M] §2）

[S]が最も簡潔に示す基本アイディア:
- 1つの基本ブロックにつき1つの関数を定義する。
- 各関数は「ブロック入口で生きている変数の値」を引数として受け取る。
- ブロック間のジャンプ（goto）は、その関数への**末尾呼び出し**に変換する。
- これにより**破壊的代入が関数的な束縛（引数の束縛）に変換される**。

[S]の実例（テキスト表記。10個の整数の積を求めるループ）:

```
変換元（命令型）:
  L0: r = 1; i = 0;
  L1: if i = 10 then return r;
  L2: r = r * a[i]; i = i + 1; goto L1;

変換先（関数型）:
  f0(a, r, i) = let r = 1 in let i = 0 in f1(a, r, i)
  f1(a, r, i) = if i = 10 then r else f2(a, r, i)
  f2(a, r, i) = let r = r * a[i] in let i = i + 1 in f1(a, r, i)
```

注目点: 基本ブロックの定義（入口は先頭のみ・出口は末尾のみ）から、**すべてのジャンプと分岐は自動的に末尾位置**にあり、変換後の関数呼び出しもすべて末尾呼び出しになる。`[流用: §5.2実装時 — while/for相当ブロックの変換アルゴリズムの骨格そのもの]`

### 1-2. 【指示書§5.1の修正を要する重要点】ループ先頭のφ関数 = 関数の引数

3論文が一致して示す対応（[K] §4 G_jump の定義、[M] §8.1 の phi→f(x*) 規則、[S] 上記例のf1の引数）:

- **合流点（join point）のφ関数は、その合流点に対応する関数の「仮引数」に変換される。**
- φ関数への各流入値（どのブロックから来たかで選ばれる値）は、各呼び出し側での「実引数」に変換される。
- [K]の表現では: λ_jump（ブロックに対応する継続）の引数が、SSAのφ関数の引数に正確に対応する（"The arguments to the λ_jump's ... become the arguments to the φ-function"）。
- [M]では goto はジャンプ先の実引数情報を保持しないため、補助関数 `argv(g, f)`（呼び出し元ラベルg・呼び出し先ラベルfから実引数列を返す）を導入して形式化している。

**本システムへの含意（指示書の精密化）:**
指示書§5.1の「φ関数は三項演算子の返り値そのものが担う」は、**If-Elseが値として合流する場合（diamond型の合流）に限り正しい**。しかしループ（§5.2）の場合、ループ先頭のφ（初回は初期値、2周目以降は更新値を受け取る）は三項演算子では表現できず、**letrec関数の仮引数として表現するのが正しい**。すなわち:

- **If-Elseの合流のφ** → 三項演算子の返り値（指示書§5.1のまま。追加ノード不要）
- **ループ先頭のφ** → letrec自己参照関数の仮引数（ループ変数 = 引数。初回呼び出しの実引数 = 初期値、末尾自己呼び出しの実引数 = 更新値）

指示書§5.2にこの規則を明記追加すること。`[流用: §5.2実装時 — 必須の設計規則]`

### 1-3. Blockly入力ゆえの簡略化（[K][M]の支配木アルゴリズムは不要）

[K] §5と[M] §5-6は、**非構造化な制御フロー（任意のgoto）**を持つSSAから関数型への変換において「letrec関数をどこに配置すればスコープが正しくなるか」を支配木（dominator tree）で決定し、その正しさを証明している（[K]の定理: Hが生成するCPSプログラムの全変数使用は正しくスコープ内にある。証明は支配関係が束縛形式に対応することによる）。[M]は基本ブロック単位のラベル付けによりletrecが1つで済み配置が自明になることを貢献として主張する。

**本システムへの含意**: Blocklyのブロックは**構造化制御フロー**（while/ifはブロックの入れ子）であり、任意のgotoが存在しない。構造化プログラムでは**支配木 = 構文木の入れ子構造そのもの**であるため、[K][M]の一般的な支配木計算・配置アルゴリズムは不要で、**構文の入れ子に沿ってletrecを生成すればスコープの正しさが自動的に保証される**。これは[M]の「基本ブロック単位ラベル付けで配置が自明になる」という簡略化のさらに強いケースに当たる。transpiler.tsの実装ではこの事実をコメントで明記し、一般アルゴリズムを実装しない根拠とすること。`[流用: 直接 — 実装しないことの理論的根拠]`

### 1-4. 自由変数によるパラメータ最小化（[M] §3, [S] §7）

[S]の素朴な変換は「スコープ内の全変数を全ブロックに渡す」ため無駄が多い（[S]自身が§7で認め、Kobayashiの型ベース不要変数除去を参照）。[M]はこの問題への解決として、**ループで変化する変数だけを引数にし、不変な変数は自由変数（外側スコープからの捕捉）として参照する**形式を採用し、これを研究の主要な貢献としている。

**本システムへの含意**: §5.2でループをletrec関数に変換する際、
- **ループ変数（ループ内で再代入される変数）** → 関数の仮引数
- **ループ不変変数** → 引数にせず、外側のlet束縛を自由変数として参照

とする。これは可視化上も重要で、不要な引数を渡すとグラフのエッジ数が不必要に増える（指示書§4.5の線形性指標を悪化させる）。自由変数として参照される変数は、Weck & Tichy原則4-2（部分グラフへの外→内エッジ = クロージャの自由変数捕捉）でそのまま可視化できるため、視覚語彙とも整合する。`[流用: §5.2実装時]`

---

## 2. 正しさ証明のテンプレート（[S] §3-5 + 付録）

[S]は本プロジェクトの「変換の意味保存を構成的に示す」という要件（指示書§4.7、および研究価値議論での意味保存 ⟦P⟧=⟦T(P)⟧ の要請）に対する、**そのまま使える証明の設計図**を与える。

[S]の証明構成（4ステップ）:
1. **変換元言語の操作的意味論**を実行状態の書き換え規則で定義する（[S]図2。状態 = プログラム・現在ブロック・ストアσ・スタック等の組）。
2. **変換先言語の操作的意味論**を同様に定義する（[S]図6。状態 = プログラム・式・環境ρ）。
3. **適格性（well-formedness）述語**を定義し、「適格な状態から一意に次状態が定まり、適格性が保存される」ことを示す（[S]定理1）。
4. **実行状態の変換 T** を定義し、**シミュレーション定理**「S が適格かつ S → S' ならば T(S) →* T(S')」を証明する（[S]定理2。証明は書き換え規則ごとの場合分け）。系として「元プログラムが値iで停止 ⇔ 変換後も同じ値で停止／両方無限ループ」（[S]系3）が得られる。

**本システムへの含意**:
- 変換元 = 手続型AST（extractor出力）の操作的意味論、変換先 = 関数型AST（§4.2）の操作的意味論（= Launchbury規則、§4.7で対応付け済み）として、この4ステップをそのまま踏襲できる。
- ただし[S]はcall-by-value同士のシミュレーションである。本システムは「手続型（正格な逐次実行の直感）→ call-by-need」なので、シミュレーション関係は「観測等価（コンソール出力列の一致）」レベルで立てるのが現実的（内部ステップは1対1対応しないため）。これは指示書§4.6のGHC差分テスト（Print列の突き合わせ）が検証している性質と正確に一致する。**§4.7の対応表を書く際、[S]のT(S)（実行状態の変換）に相当する対応を「手続型ASTの実行状態 ↔ 関数型ASTの評価状態＋トレース」として定義しておくと、将来の形式的証明への昇格経路が確保される。** `[流用: 検証用 — §4.7の証明方法論のテンプレート]`
- [S]定理1の「適格性」の発想（変数・ラベル・関数名の宣言に不足や重複がないことの機械的検査）は、transpiler.tsの出力に対する**ユニットテストの検査項目**としてそのまま流用できる（生成された関数型ASTの全Varが束縛済みであること、SSA名に重複がないこと等）。`[流用: 直接 — テスト設計]`

---

## 3. CPS導入を遅延させた判断の裏付けと、導入時の正確な境界（[K] §6, [S] §2.2）

本システムはCPSをデフォルト採用せず direct style を選んだ（指示書§2）。3論文はこの判断の正当性と、**将来CPSが本当に必要になる正確な条件**を与える。

- [S] §2.2: 継続の導入が必要になるのは「**末尾でない関数呼び出し**」が現れたときである。基本ブロック変換（本書§1-1）だけでは末尾でない呼び出しを除去できず、そこで初めて継続kを導入する。逆に言えば、**すべての呼び出しが末尾位置にある限り、継続は一切不要**。
- [K] §6: CPSとSSAの対応が破綻するのは、非局所リターン（Cのlongjmp、Schemeのcall/cc）が現れたとき。継続がλ_cont（呼び出しの戻り点）でもλ_jump（ブロック内ジャンプ）でもない使われ方をするため、SSAに直接対応させられなくなる。
- [K]のλアノテーション3分類は、制御構造の分類として本システムにも有用: **λ_proc**（完全な手続き呼び出し機構が必要）／**λ_cont**（呼び出しの戻り点）／**λ_jump**（gotoとしてコンパイル可能 = ループ・分岐合流）。本システムの§5.2のletrecループ関数は正確にλ_jump相当（末尾自己呼び出しのみ）であり、[K]はこれが「継続を導入せずSSA/直接スタイルで表現できる」クラスであることを保証している。

**本システムへの含意**: 指示書§2の「CPSは非局所制御が必要になる将来段階まで導入を遅延する」の「非局所制御」を、[S][K]に基づき次のように精密化できる:
「**break／ループ途中からの早期return／例外**のいずれかをブロックセットに追加する時点」がCPS（または継続の限定的導入）の境界である。それまでの while相当・if-else・再帰・高階関数は、すべて末尾呼び出し＋direct styleで表現可能（[K]のλ_jumpクラス＋[S]の基本ブロック変換で閉じる）。`[流用: 直接 — 指示書§2の「将来段階」の定義の精密化]`

---

## 4. 将来の非局所制御（break/例外）導入時のレシピ（[S] §2.3 全体）

現行スコープ外だが、[S]は例外（≒breakの一般化）を追加する際の完全なレシピと、**破壊的代入と組み合わせたときの落とし穴**を与える。ロードマップとして保管する。

- **レシピ**: 各関数は通常の継続kに加えて例外ハンドラh（継続の一種）を引数に取る。raise（break相当）はhの適用に変換。try節内からの関数呼び出しでは新しいh'を生成して渡す（[S] §2.3.2）。
- **同一関数内で解決する例外／breakは静的に分かるため、単なるgoto（= 末尾呼び出し）に変換してよい**（[S] §2.3.1）。ループのbreakはほぼ常にこのケースであり、**実はCPS全面導入なしに「break先ブロックへの末尾呼び出し」で処理できる**可能性が高い。CPS導入前にまずこの静的解決を検討すること。
- **落とし穴（[S] §2.3.3）**: 破壊的代入のある言語で例外ハンドラをクロージャとして素朴に生成すると、**ハンドラ生成時点の変数値がクロージャに捕捉され、raise時点の値と食い違う**バグが生じる。[S]はこれを内部/外部例外の区別で回避した。**本システムはSSA（各バージョンが別名）を採用しているため、この問題は構造的に発生しない**——クロージャが捕捉するSSA名は一意に1回だけ束縛されるので、「捕捉時の値」と「使用時の値」の乖離が原理的に起きない。これは本システムのSSA設計の健全性を裏付ける独立した証拠として、指示書のコメント/ドキュメントに記録する価値がある。`[流用: 将来 ＋ SSA設計の妥当性の裏付けとして直接]`

---

## 5. 【本システム固有の注意】call-by-value前提の変換をcall-by-needに持ち込む際の空間リーク

3論文の変換はすべてcall-by-value前提であり、そこでは意識されない問題が、本システムのcall-by-need（指示書§3）では顕在化する。**これは3論文には書かれていない、本システムが独自に対処すべき交差点の問題である。**

- 本書§1-1のスキームでループをletrec末尾再帰に変換すると、アキュムレータ（例: カウンタc、積r）は各反復で `c_new = c + 1` のような**Thunk**として次の反復へ渡される。call-by-needでは需要が届くまでforceされないため、n回のループで `((((c+1)+1)+1)...+1)` という**深さnの未評価Thunk連鎖**がヒープに積み上がる。これはHaskellで古典的に知られる `foldl` の空間リーク（`foldl` vs `foldl'`、seqによる正格化）と同型の問題である。
- **本システムでの現れ方は二面的**:
  - (a) **可視化上**: Thunk連鎖はゴーストノードの長い鎖としてグラフに現れる。これは「遅延評価では計算が必要になるまで積み上がる」ことの**教育的に正確な可視化**であり、needが最後に届いた瞬間に鎖が一気にforceされる様子はcall-by-needの本質を見せる好機でもある。
  - (b) **実行・描画性能上**: 反復回数が大きいとノード数がブロック数に対し**超線形**に増え、指示書§4.5の線形性指標に抵触し、深いforce連鎖はスタックオーバーフローのリスクもある。
- **推奨する設計判断**: 教育目的（小さい反復回数）では(a)を活かしてそのまま可視化し、反復回数の上限（例: 可視化は数十回まで、それ以上は集約表示）を設ける。将来的に性能が問題になる場合は、ループアキュムレータに限定した正格化（Haskellの `foldl'`/bang patternに相当する「引数をWHNFまでforceしてから束縛する」オプション）を評価器に追加する余地を残す。ただしこれは§3の「全値はThunk」という統一原則への例外となるため、導入時は指示書§3への明記を必須とする。`[流用: 直接 — §5.2実装前に方針決定が必要]`

---

## 6. 指示書への具体的な反映事項（サマリ）

1. **§5.1に追記**: 「φ = 三項演算子の返り値」はIf-Elseの値合流に限る。ループ先頭のφはletrec関数の仮引数として表現する（本書§1-2）。
2. **§5.2に追記**: while相当ブロックの変換規則を明文化——(a) ループ本体 = letrec自己参照関数、(b) ループ変数（再代入される変数）= 仮引数、初期値 = 初回呼び出しの実引数、更新値 = 末尾自己呼び出しの実引数、(c) ループ不変変数 = 自由変数として外側スコープから捕捉（本書§1-1, §1-4）、(d) Blocklyの構造化入力ゆえ支配木計算は不要で構文の入れ子に従う（本書§1-3）、(e) アキュムレータのThunk連鎖への方針（可視化上限 or 正格化オプション）を決定しておく（本書§5）。
3. **§2の精密化**: 「CPS導入を遅延する将来段階」= break／早期return／例外の追加時点と定義。ただしループ内breakは静的解決（末尾呼び出し化）でCPSなしに処理できる可能性をまず検討（本書§3, §4）。
4. **§4.7に追記**: [S]の4ステップ証明構成（両言語の操作的意味論→適格性→状態変換T→シミュレーション定理）を、意味保存の構成的検証のテンプレートとして採用。現段階では観測等価（Print列一致 = GHC差分テスト）レベルで運用し、形式証明への昇格経路を確保（本書§2）。
5. **transpiler.tsのテストに追加**: [S]の適格性述語に倣い、生成された関数型ASTの全Var束縛済み・SSA名非重複の機械的検査（本書§2）。
6. **ドキュメントに記録**: SSA採用により[S] §2.3.3の「クロージャが古い変数値を捕捉する」バグクラスが構造的に排除されることを、設計妥当性の裏付けとして明記（本書§4）。