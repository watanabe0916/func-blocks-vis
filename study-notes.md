# 本プロジェクト 完全学習ノート（発表・質疑応答のための知識体系）

> このファイルはコミットしない個人用学習ノート。プロジェクトを他人に説明・発表し、
> 質問に完璧に答えるために知っておくべき事項を、専門用語を第一原理から網羅的にまとめる。
> 各章末に「発表で言えると強い一言」と「想定質問」を付す。

---

## 第0章 このプロジェクトを一文で言うと

**「Blocklyで手続型（順次・代入）のプログラムを組ませ、それを裏側で純粋関数型プログラム（明示的な関数型AST）に変換し、遅延評価（call-by-need）の純粋評価器で実行し、その評価過程をデータフローグラフとして可視化する、プログラミング初学者向けのWeb学習環境。」**

一段深い一文：
「手続型のメンタルモデルを入口にしながら、裏で関数型として動かして見せることで、遅延評価・参照透過性・再帰といった関数型の利点を"体験"させ、`eval()`で文字列コードを実行するのではなく、意味論的に定義された独自パイプラインで実行する。」

**新規性（研究上の立ち位置）**：
- Weck & Tichy 系の研究＝「データフロー構造の静的可視化（高レベルのプログラム理解）」
- ghc-vis 系の研究＝「遅延評価と共有（sharing）の動的可視化」
- **本システムはこの2つの中間**。前者の視覚語彙（ノード/エッジ/型/部分グラフ）を骨格に、後者の関心事（force済み/未評価ゴースト状態）を状態レイヤとして重ねて統合する。この統合が既存研究にない立ち位置。

---

## 第1章 コンパイラ・言語理論の基礎用語

発表では「コンパイラの一般的な用語」を正しく使えることが信頼につながる。

### 1.1 AST（抽象構文木, Abstract Syntax Tree）
- プログラムの構造を木で表したデータ構造。「抽象」とは、括弧やセミコロンなど構文上だけ必要な要素を落とし、意味のある構造だけを残すこと。
- 例：`x + 1` は `{ 演算: +, 左: x, 右: 1 }` という木のノードになる。
- 本システムには**2種類のAST**がある：
  - **手続型AST**（`src/compiler/types.ts`）：Blocklyブロックの構造をそのまま写したもの。`Assign`（代入）/`Print`/`If`/`While`/`Apply`（演算）等。
  - **関数型AST**（`src/compiler/functionalAst.ts`）：変換後の純粋関数型プログラム。`Let`/`LetRec`/`If`/`PrimApp`/`Apply`/`Pair`/`Proj`/`Do` 等。
- **判別フィールド**：手続型ASTは `type`、関数型ASTは `kind` で種別を区別する（混同防止のため意図的に別名）。TypeScriptの「タグ付きユニオン（discriminated union）」という手法。

### 1.2 パーサ／ジェネレータ／トランスパイラ／エバリュエータ
- **抽出（extraction）**：Blocklyワークスペース → 手続型AST。担当は `extractor.ts`。BlocklyのAPIでブロックを辿ってJSONの木を作る。
- **トランスパイラ（transpiler）**：あるASTを別のASTへ変換する。担当は `transpiler.ts`。手続型AST → 関数型AST。「コンパイラ（機械語まで落とす）」と区別して、同じ抽象度の言語間変換を transpile と呼ぶ。
- **評価器（evaluator / interpreter）**：ASTを解釈実行して値を出す。担当は `evaluator.ts`。
- **プリティプリンタ（pretty printer）**：ASTを人間可読なテキスト（今回はHaskellソース）に戻す。担当は `haskellPrinter.ts`。

### 1.3 手続型パラダイム vs 関数型パラダイム
- **手続型（imperative / procedural）**：状態（変数）を破壊的に書き換えながら、命令を順に実行する。「`x = x + 1`」は「xの中身を更新せよ」という命令。メンタルモデルは「メモリを書き換える」。
- **関数型（functional）**：値の変換を関数適用で記述する。変数は一度束縛したら変えない（不変, immutable）。「`x = x + 1`」は許されず、代わりに「新しい名前 `x2 = x1 + 1`」を作る。メンタルモデルは「式を値に簡約する」。
- 本システムの肝は**「手続型で書かせて関数型に変換する」**こと。この変換を成立させる中心技術がSSA（第3章）。

### 1.4 操作的意味論と表示的意味論
- **操作的意味論（operational semantics）**：「プログラムがどう実行されるか」を状態の書き換え規則で定義する。Launchburyの規則（第4章）はこれ。
- **表示的意味論（denotational semantics）**：「プログラムがどんな数学的対象（値・関数）を表すか」を定義する。`⟦P⟧`（プログラムPの意味）という記法。
- 「意味保存（meaning preservation）」＝変換前後で意味が等しい、`⟦P⟧ = ⟦T(P)⟧`。本システムはこれを**観測等価（第9章）**のレベルで検証する。

**発表で言えると強い一言**：「AST を2つ持ち、手続型 AST から関数型 AST への変換を独立したステージにしてあるので、"独立したテキスト形式の関数型プログラムの実体"が存在します。これが後述の GHC 検証や意味保存の議論を可能にしています。」

**想定質問**：
- Q「なぜ `eval()` を使わないのか？」→ A「(1) 文字列コードの直接実行はセキュリティ・意味論の明示性の点で不適。(2) 本研究の目的は"評価過程の可視化"であり、評価器を自作しないとトレース（評価の各ステップ記録）が取れない。(3) 独自の意味論（call-by-need）を保証するには自前の評価器が必須。」

---

## 第2章 5段階パイプライン・アーキテクチャ

### 2.1 5つのステージ
```
Blocklyワークスペース
 → ① 手続型AST抽出        extractor.ts        （既存）
 → ② 関数型ASTへの変換     transpiler.ts       （変換のみ）
 → ③ 純粋評価器による実行   evaluator.ts        → { trace, consoleOutput }
 → ④ 結果表示             consoleOutput       → 仮想コンソール
 → ⑤ トレース駆動グラフ描画 renderGraph.ts      → (nodes, edges) → React Flow
```
- ④と⑤は③の出力（trace）を共有する「兄弟」。互いに依存しない。

### 2.2 なぜ「グラフ構築」と「評価」を融合させてはいけないか（設計の核心）
旧版は「評価しながら同時にグラフのノードを書き換える」1段階アーキテクチャだった。これを禁じ、分離した理由：
1. **証明の強度**：独立したテキスト形式の関数型プログラムがないと、call-by-need意味論の"構成的証明"の主張が弱くなる。
2. **将来の再利用**：グラフ構築と評価が融合していると、再帰（§5.2）・高階関数（§5.4）で必要な「再利用可能な関数定義」を表現できない。
3. **対応関係論証**：評価器を独立させることで、Launchburyの規則との対応（第4章）を論じられる。

### 2.3 純粋関数としての各ステージ
- `evaluate(program)` は**UIに一切依存しない純粋関数**。返すのは `{ trace, consoleOutput }` だけで、React Flowのノードを直接いじらない。
- `renderGraph(program, trace)` は**評価を一切行わない純粋関数**。trace を読むだけでグラフを組む。
- この分離により、②③⑤を**それぞれ単体テスト**できる（`transpiler.test.ts` / `evaluator.test.ts` / `renderGraph.test.ts`）。

**発表で言えると強い一言**：「評価とUIを完全に分離し、評価器は trace（評価イベントの列）だけを出力します。可視化はその trace のみを入力とする純関数なので、"評価中にノードを書き換える"ようなライブ変異への依存がありません。」

**想定質問**：
- Q「trace とは何を記録しているのか？」→ A「force（Thunkの強制評価）・print・error の3種のイベント。各イベントは対象ノードID・順序（order）・結果値・メモ化ヒットか否か等を持つ。これが可視化の唯一の情報源。」

---

## 第3章 SSA（静的単一代入）と φ関数

### 3.1 SSA（Static Single Assignment form）とは
- **各変数が「ちょうど一度だけ」代入される**ように書き換えた中間表現。コンパイラ最適化で広く使われる。
- 手続型の `x = 1; x = x + 1;` を、SSAでは `x_1 = 1; x_2 = x_1 + 1;` と**バージョン番号**を振って別名にする。
- 「再代入」を「新しい名前の束縛」に変える＝**破壊的代入を不変束縛に変換する**中心アイデア。これで手続型を関数型に変換できる。
- 本システムでの呼び名：「新しい環境（スコープ）の生成とシャドウイング」。

### 3.2 φ（ファイ, phi）関数
- 制御フローが**合流する点**で、「どの経路から来たかによって値が変わる」ことを表す仮想的な関数。
- 例：`if c then x=10 else x=20` の後、`x` は10か20か。SSAでは `x_1=10`, `x_2=20` として、合流点で `x_3 = φ(x_1, x_2)` と書く。φは「どっちかを選ぶ」。
- **本システムでの実装**（重要な区別、transform.mdの精密化）：
  - **If-Else の合流（diamond型）のφ** → **三項演算子 `cond ? a : b` の返り値**が担う。専用ノード不要。（Appel「SSA is Functional Programming」の対応）
  - **ループ先頭のφ** → **三項演算子では表現できない**。`letrec`（自己参照関数）の**仮引数**が担う。初回は初期値、2周目以降は更新値を受け取るため。

### 3.3 Appel「SSA is Functional Programming」
- Andrew Appel の有名な論文/主張：「SSAは関数型プログラミングそのものである」。
- φ関数＝関数の引数、SSAのブロック＝関数、というふうに、SSAと関数型（特にCPSや相互再帰関数）は1対1対応する。
- 本システムが「手続型→SSA→関数型」という変換で理論的破綻がないことの後ろ盾。

### 3.4 適格性（well-formedness）述語
- 変換器が出力した関数型ASTが「壊れていない」ことの機械的チェック。
- [S]（住井ら）論文の適格性述語は「全変数が束縛済み・宣言に重複がない」。
- **本システムはこれをそのまま採用しない**：本システムは未束縛変数を⊥（第4章）として意図的に許容するため。採用する不変条件は**「生成されたSSA束縛名に重複がないこと」だけ**。これは `transpiler.test.ts` でテスト済み。

**発表で言えると強い一言**：「再代入をSSA（バージョン番号による別名化）で扱うので、破壊的代入が構造的に排除されます。しかも一度しか束縛されない名前なので、将来 break/例外を入れても"クロージャが古い値を捕まえる"古典的バグが原理的に起きません。」

**想定質問**：
- Q「if-else と while でφの扱いが違うのはなぜ？」→ A「diamond合流は2つの値のどちらかを選ぶだけなので三項演算子の返り値で表せる。ループ先頭は"初回は初期値・以降は前回更新値"という自己参照的な値を受けるので、三項演算子では表現できず、再帰関数の仮引数にする必要がある。これは3論文（Kelsey/森下/住井）が一致して示す知見。」

---

## 第4章 【最重要】Call-by-Need（真の遅延評価）

このプロジェクトの理論的な核。ここを完璧にできるかが発表の成否を分ける。

### 4.1 評価戦略の3分類
- **Call-by-value（値渡し, 正格評価 / strict）**：関数に渡す前に引数を**必ず先に評価**する。C・Java・JS・OCamlなど大半の言語。手続型の直感もこれ。
- **Call-by-name（名前渡し）**：引数を評価せずそのまま渡し、**使うたびに毎回評価**する。同じ引数を2回使うと2回計算される（非効率）。
- **Call-by-need（必要渡し, 遅延評価 / lazy）**：引数を評価せず渡すが、**初めて必要になった時に1回だけ評価し、結果をキャッシュ**して以降は使い回す。＝ **call-by-name + 共有（sharing）**。Haskellの評価戦略。**本システムはこれをデフォルトの評価規則として全体に保証する。**

### 4.2 Thunk（サンク, 遅延セル）
- 「まだ評価していない計算」を包む箱。本システムでは有限状態機械（FSM）として実装：
  - `unevaluated`（未評価）：計算を再現するクロージャ `() => Value` を保持。
  - `forcing`（評価中）：§5.2で追加した過渡状態。ブラックホール検出用（後述4.9）。
  - `evaluated`（評価済み）：force済みの結果値をメモ化保持。
- 「全ての値はThunkとして生成される」＝評価結果は生の値ではなく常にThunk。

### 4.3 force（強制）と WHNF
- **force**：Thunkを実際に評価して値を取り出す操作。
- **WHNF（Weak Head Normal Form, 弱冠頭正規形）**：force は**WHNFまでしか評価しない**。すなわち、いちばん外側のコンストラクタ（`cons`セルや`Pair`）の殻だけを暴き、**内部の引数Thunkは未評価のまま残す**。
  - 対比：**NF（Normal Form, 完全正規形）**は中身まで全部評価した形。
  - 例：`Pair(1+1, 2+2)` を force すると `Pair(<thunk>, <thunk>)` まで（外側の対だけ判明、中身は未評価）。
- **なぜWHNFが重要か**：この「頭だけ暴く」性質が、**無限リスト（余再帰）を有限計算で扱える**理論的根拠。tailを常にThunkのまま残せるから、`take 3` で必要な分だけ force できる。

### 4.4 メモ化（sharing）と「観測的に純粋」
- Thunkは一度 force したら結果をキャッシュし、以降は再計算せず共有値を返す。これが call-by-need の "need"。
- **メモ化がないと（＝call-by-name）**：同一Thunkへの複数需要で計算量が指数爆発しうる。だから sharing は必須要件。
- **可変セルなのに"純粋"と言える理由**：force() は同じThunkに同じ値を返す純関数として振る舞い、「メモ化されている」事実そのものは外から観測できない。この「観測不能な副作用」を **benign effect（良性の副作用）** と呼ぶ。だから「純粋関数型」の前提を破らない。コードにこの根拠を明記してある。

### 4.5 需要駆動（demand-driven）と「需要の根」
- 遅延評価では「何が評価を始動するか（強制点）」を決める必要がある。
- 本システムでは**仮想コンソールへの出力（Printの列＝`Do`ノード）を「需要の根（root of the demand spine）」**とする。
- 「印字する必要がある」という需要が計算グラフの根から末端へ伝播し、**必要なThunkだけ**が force される。
- 結果：**需要が届かなかったThunkは永遠に未評価のまま残る**。これがトレースに記録され、可視化で「計算がスキップされた」ことをそのまま見せる。

### 4.6 短絡評価は「創発する」
- `and`, `or` を**特殊構文にしない**。普通の関数として `and = (x,y) => x ? y : false` と定義する。
- 全引数はもともとThunkなので、`x` が false のとき `y` には need が届かず force されない。**短絡評価は遅延評価の自然な帰結として創発**する。
- だから評価器に短絡専用の特殊分岐は一切ない。`And`/`Or` は普通の `PrimApp`（第一級関数適用）として一貫表現できる。
- 同様に **if-then-else も特殊規則不要**：scrutinee（条件）だけ force し、選ばれた枝だけ force する、で非正格性が創発する。

### 4.7 Bottom（⊥, ボトム）と非停止
- **⊥（ボトム）**：「値を持たない計算」＝非停止（無限ループ）やエラーを表す、全ての型に属する特別な値。
- 遅延評価では、未評価Thunkが⊥を包んでいても、**force されない限りプログラム全体は停止しうる**。
- 例：`false && (未定義変数 y > 0)` は、右辺が⊥（未束縛変数の参照）でも、左辺falseで短絡して need が届かず、安全に false に簡約される。クラッシュしない。
- 本システムは**未束縛変数の参照を⊥として意図的に許容する**（正格言語ならエラーで即停止するところ）。

### 4.8 Launchbury の自然意味論
- **John Launchbury, "A Natural Semantics for Lazy Evaluation", POPL 1993**。
- call-by-need を**ヒープ（heap, 変数→式の対応）付きの操作的意味論**で厳密に定義した記念碑的論文。本システムの評価器の理論的基礎。
- 主要な評価規則（`launchbury.md` に対応表がある）：
  - **LIT規則**：リテラルはそのまま値。
  - **VAR規則**：変数参照。ヒープから束縛を引き、評価し、**結果でヒープを更新（＝メモ化そのもの）**。`Γ[x↦e], x ⇓ Δ[x↦v], v`。
  - **LET規則**：束縛をヒープに置くだけ（評価しない）。もともと**相互再帰的な束縛（letrec）**を扱える。
  - **APP規則**：関数適用。引数で環境を拡張して本体を評価。
  - **本システムの拡張**：`If`↔CASE規則のBool特殊化、`PrimApp`↔PRIMOP拡張規則。
- **Wadsworth のグラフ簡約（graph reduction）**：Christopher Wadsworth が博士論文（1971）で示した、λ式を**グラフとして共有付きで簡約**する手法。call-by-need の "sharing" の源流。本システムの理論的背景。

### 4.9 ブラックホール（black hole）検出
- letrec（自己参照）を導入すると、「評価中の値を、その評価の途中でまた必要とする」循環が起こりうる（`x = x` のような）。これは⊥。
- Launchbury の VAR 規則は「束縛の評価中、その束縛を**ヒープから一時的に取り除く**」。評価中の再参照は導出不能＝ブラックホール。
- 本システムは Thunk の過渡状態 `forcing` でこれを表現し、`forcing` 中の Thunk への force を制御されたエラーにする。
- 現行の変換（while由来のletrecは関数束縛のみ）では到達しないが、防御的に先行導入。§5.3（値レベル自己参照）で本質的になる。

**発表で言えると強い一言**：「全ての値をThunk（遅延セル）として生成し、force はWHNF（外側の殻だけ）までしか評価しません。メモ化により call-by-name の指数爆発を防ぎつつ、その可変性は観測不能なので参照透過性を破りません。短絡評価も if も、特殊構文ではなく遅延評価から自然に創発します。」

**想定質問**：
- Q「メモ化は副作用では？純粋と言えるのか？」→ A「force() は同一Thunkに同一値を返す純関数で、メモ化の有無は外から観測できない。この benign effect は参照透過性を破らない。Launchbury の VAR 規則のヒープ更新に正確に対応する。」
- Q「なぜ call-by-name ではダメ？」→ A「同じThunkへの複数需要で再計算が起き、計算量が指数爆発しうる。sharing（メモ化）は必須要件。」
- Q「WHNF と NF の違いを説明して」→ A「WHNFは最外コンストラクタの殻だけ暴く。NFは中身まで全評価。WHNFに留めるからこそ無限リストのtailをThunkのまま残せて有限計算で扱える。」
- Q「無限ループするプログラムはどうなる？」→ A「⊥として扱う。需要が届かなければ停止する。届く場合は評価器の適用回数上限（500回）で制御されたエラーにする（意味論の一部ではなくリソース保護）。」

---

## 第5章 変換規則（transpiler の中身）

### 5.1 if-else の変換（§5.1）
- `cond ? a : b`（三項演算子＝`If`ノード）へ変換。φ＝Ifの返り値（diamond合流）。
- **束縛の巻き上げ**：分岐内の代入はいったん全部、合流点の前に平坦なLet連鎖として並べる。
  - 正格言語なら不健全（両枝を評価してしまう）だが、**call-by-needでは束縛≠評価**（LET規則はヒープ割り当てのみ）なので健全。選ばれなかった枝の束縛はゴーストのまま残り、可視化で「スキップ」が見える。
- **条件式の共有**：φが複数あるとき、条件式を合成束縛 `%cond_n` として一度だけlet束縛し、各φから変数参照で共有。複製すると sharing 違反＋グラフのノード重複になるため。Launchburyの正規化「適用の引数は変数でなければならない」への対応。
- **片側代入**：then側でしか代入されない変数の else 腕は、分岐前の版（未代入なら版0＝未束縛⊥）を参照。手続型の「その経路では未定義」と正確に一致。

### 5.2 while の変換（§5.2）
- **letrec（自己参照関数 `%loop_n`）＋末尾自己呼び出し**へ変換。（基本ブロック＝関数、ジャンプ＝末尾呼び出し）
- **ループ変数**（本体内で再代入される変数）＝**関数の仮引数**（＝ループ先頭のφ）。初回呼び出しの実引数＝突入前の値、末尾呼び出しの実引数＝周回末尾の更新値。
- **ループ不変変数**（再代入されない変数）＝仮引数にせず、**外側スコープの自由変数として閉包に捕捉**。エッジ数の線形性にも寄与（森下[M]の主要貢献）。
- **本体内のSSA束縛**は反復ごとに評価されるため、プログラムレベルに巻き上げず**式レベルの `LetIn`** で表現。
- **複数ループ変数の出口値**：入れ子の**対（Pair）** で返し、適用結果を `%r_n` として一度だけlet束縛してから**射影（Proj = fst/snd）** で取り出す。射影式が複製されても対の中の同一Thunkを共有するため再計算なし。
- **支配木（dominator tree）計算は不要**：Blocklyは構造化制御フロー（任意のgotoがない）なので、支配木＝構文木の入れ子そのもの。構文の入れ子に沿ってletrecを生成すればスコープの正しさが自動保証される。[K][M]の一般アルゴリズムを実装しない根拠。

### 5.3 direct style と CPS（継続渡しスタイル）
- **direct style（直接スタイル）**：関数が値を「返す」普通の書き方。本システムはこれ＋ネストしたlet。
- **CPS（Continuation Passing Style, 継続渡しスタイル）**：関数が値を返す代わりに「続きの計算（継続, continuation）」を引数kで受け取り、`k(結果)` を呼ぶ書き方。制御フローを明示的に扱える。
- **本システムはCPSをデフォルト採用しない**。CPSが必要になる正確な境界＝**break／ループ途中からの早期return／例外**をブロックに追加する時点（＝「非局所制御」）。それまでのwhile・if-else・再帰・高階関数はすべて末尾呼び出し＋direct styleで表現できる。
- ただしループ内breakの多くは、ジャンプ先が静的に一意なので「末尾呼び出し」として処理できる（CPS全面導入前にまず静的解決を検討）。

### 5.4 チューリング完全性（Turing completeness）
- 計算能力を保証するのは **「一般再帰（自己参照letrec, §5.2）」＋「実行時値に依存する条件分岐（§5.1）」** のみ。
- **map/filter/reduce等の高階関数、遅延評価それ自体は計算能力を追加しない**。これらは「モジュール性・利便性・教育効果」の層。
- **原始再帰 vs 一般再帰**：
  - **原始再帰関数（primitive recursive）**：有界ループ（回数が事前に決まる `for`）で表現できる関数。**必ず停止する**が、表現力に限界（アッカーマン関数は表現不可）。
  - **一般再帰関数（general recursive）**：`while`（停止するか事前に分からないループ）で表現できる関数。停止しないこともある＝チューリング完全。
  - **Meyer & Ritchie**：`loop`言語（有界forのみ）＝原始再帰まで、`while`言語＝チューリング完全、という古典的結果。だから「有界forだけでは不十分、whileが要る」。

### 5.5 再帰（recursion）vs 余再帰（corecursion）
- **再帰（帰納, induction）**：停止する計算。基底ケースへ向かって縮んでいく（例：階乗）。
- **余再帰（余帰納, corecursion）**：**停止しない**が、WHNF＋tail Thunk＋`take`で有限化できる計算。無限リスト生成（`iterate(f,x0) = cons(x0, iterate(f, f(x0)))`）など。
- 両者は同じ自己参照束縛ブロックで書けるが**概念的に別物**。チューリング完全性（停止性）の議論と混同しないよう、コメントに「recursion ではなく corecursion」と明記する方針。

**発表で言えると強い一言**：「while を letrec の自己参照関数へ、ループ変数を仮引数へ変換します。Blocklyは構造化制御フローなので、コンパイラ理論で普通必要になる支配木計算が不要で、構文の入れ子にそのまま従えばスコープが正しくなります。」

**想定質問**：
- Q「なぜ CPS を使わないのか？」→ A「現行スコープ（while/if/再帰/高階）は全て末尾呼び出し＋直接スタイルで表現でき、CPSが必要なのは break/早期return/例外という非局所制御を入れる時だけ。3論文（特に住井[S]§2.2）がこの境界を厳密に規定している。不要な複雑さを避けた。」
- Q「有界 for だけでいいのでは？」→ A「有界forは原始再帰までしか表現できない（Meyer & Ritchie）。チューリング完全にするには一般再帰＝while相当が必要。」
- Q「無限リストをどう有限で扱う？」→ A「WHNF強制で頭だけ暴き、tailをThunkのまま残す。`take n`で必要な分だけforceする。これが余再帰。」

---

## 第6章 手続型→関数型変換の先行研究（transform.md の3論文）

発表で「先行研究を踏まえている」と言えると学術的信頼が上がる。

### 6.1 [K] Kelsey 1995「CPSとSSAの対応」
- "A Correspondence between Continuation Passing Style and Static Single Assignment Form"。
- **CPSとSSAが1対1対応する**ことを示した。φ関数＝継続（λ_jump）の引数。
- λの3分類：**λ_proc**（完全な手続き呼び出し）／**λ_cont**（呼び出しの戻り点）／**λ_jump**（gotoとしてコンパイル可能＝ループ・分岐合流）。本システムのループ関数は λ_jump 相当（末尾自己呼び出しのみ）で、「継続なしで表現できるクラス」であることが保証される。
- CPS↔SSAの対応が破綻するのは非局所リターン（longjmp, call/cc）が現れたとき。

### 6.2 [M] 森下大輔 2013「手続き型と関数型の相互変換」
- 直接スタイル（OCamlサブセット）をターゲットとし、**本システムに最も近い**。
- 主要貢献：**ループ不変変数を自由変数として捕捉し、引数を最小化**する（本システム§5.2(c)に採用）。
- 基本ブロック単位ラベル付けで letrec 配置が自明になる。

### 6.3 [S] 住井英二郎ら 2004「例外処理機構を備えた命令型言語のCPS変換」
- **意味保存の4ステップ証明テンプレート**を与える（本システム§4.7に採用）：
  1. 変換元言語の操作的意味論を定義
  2. 変換先言語の操作的意味論を定義（＝Launchbury規則）
  3. 適格性述語を定義し保存を証明
  4. 実行状態の変換Tを定義しシミュレーション定理を証明
- 破壊的代入＋例外の落とし穴（クロージャが生成時点の変数値を捕捉し raise 時点と食い違う）を指摘。**本システムはSSAなのでこのバグクラスが構造的に排除される**（各SSA名は一度だけ束縛）。

### 6.4 3論文と本システムの決定的差異（必ず通すべきフィルタ）
1. **評価戦略**：3論文は全て**call-by-value**前提。本システムは**call-by-need**。変換の「構造」は流用可、「意味」の証明とアキュムレータの扱い（空間リーク）は評価戦略依存の注意が要る。
2. **変換スタイル**：[K][S]はCPS、[M]は直接スタイル。本システムは直接スタイル＋ネストしたletなので[M]が最も近い。

**想定質問**：
- Q「この変換は君のオリジナル？」→ A「変換の骨格（ブロック→関数、goto→末尾呼び出し、φ→引数）は3つの先行研究の一致した知見を採用。ただし3論文は全てcall-by-value前提で、call-by-needに持ち込むと空間リーク（foldl問題）という独自の交差点問題が生じる。ここは本システム独自の対処。」

---

## 第7章 関数型AST のノード種別（functionalAst.ts）

発表で「この木にどんなノードがある」と即答できるように。

| ノード | 意味 | 対応する手続型構造 |
|---|---|---|
| `Lit` | リテラル値（number/string/boolean） | 数値・文字列・真偽値ブロック |
| `Var` | 変数参照（SSAバージョン付き名前 `x_1`） | 変数取得ブロック |
| `PrimApp` | 固定の組込演算子への適用（算術・比較・論理） | 演算ブロック |
| `If` | 三項演算子 `cond ? then : else` | if-else（diamond合流のφ） |
| `Let` | プログラムレベルのSSA束縛（`let x_n = e in ...`） | 代入 |
| `LetIn` | 式レベルのlet（ループ本体内の束縛） | ループ本体内の代入 |
| `LetRec` | 自己参照束縛（whileの再帰関数 `%loop_n`） | while |
| `Apply` | 名前付き関数の飽和適用（`%loop_1 x_2`） | ループの呼び出し |
| `Pair` | 対（consセルの最小形。複数ループ変数の出口値） | （内部生成） |
| `Proj` | 射影 fst/snd | （内部生成） |
| `Do` | Print文の列（需要の根） | 表示ブロックの列 |
| `Print` | 個々のPrint | 表示ブロック |

- **正準ノードID**：`canonicalVarId(name) = "var_" + name`。評価器とrenderGraphが同じ規則で変数のヒープ位置／ノードを参照する。
- **`%` 接頭辞**：`%cond_1`, `%loop_1`, `%r_1` はトランスパイラが生成した合成束縛（ユーザー変数ではない）。

---

## 第8章 GHC・Haskell 検証オラクル（§4.6）

### 8.1 GHC とは
- **GHC（Glasgow Haskell Compiler）**：Haskell の標準的なコンパイラ。本プロジェクトではローカルに 9.14.1、CIに 9.6 を使用。
- **Haskell**：純粋関数型・**遅延評価（call-by-need）** の言語。本システムの評価戦略と同じなので、独立した検証オラクルとして理想的。

### 8.2 なぜ「CI専用オラクル」で本番に組み込まないか（3つの理由）
1. GHC起動・コンパイルのレイテンシがBlockly「実行」ボタンの即時フィードバック（HCI要件）を損なう。
2. 生成コードをサーバー側実行するにはサンドボックス化が必須で運用リスク増。
3. **最も本質的**：GHCの標準出力だけでは評価トレース（本研究の可視化対象そのもの）を取得できない。トレース取得には Hat/ghc-debug 級の計装が必要で、別規模の研究課題。

### 8.3 2段階の検証
1. **型検査**：`ghc -fno-code`（コード生成・リンクを省略した軽量チェック）。Blockly側の手書きバリデーションでは検出できない型不整合をGHCの型システムで機械検出。`-fno-code` は「型検査だけして実行ファイルは作らない」オプション。
2. **差分テスト（differential testing）**：`runghc`（コンパイルせずスクリプト的に実行）で実行し、標準出力を `evaluate()` の consoleOutput と**文字単位で突き合わせる**。

### 8.4 do記法とその脱糖（desugaring）
- **do記法（do-notation）**：Haskellで逐次的なIOを書く構文糖衣。`do { e; stmts }` は `e >> do { stmts }` に、`do { let d; stmts }` は `let d in do { stmts }` に**脱糖（desugar, 糖衣構文を基本構文へ展開）** される。
- 本システムの `Do`（Print列）↔ `main = do { putStrLn ...; ... }`、`Let`↔do内let、`If`↔if-then-else、`LetRec`↔letの関数束縛（Haskellのletはもともとletrec）、`Pair`↔タプル、`Proj`↔fst/snd。
- **未束縛変数 → `undefined`**：Haskellの `undefined` はまさに⊥。本システムの§3.6の意味論と正確に一致し、型検査を通過し、force時のみ実行時例外になる。

### 8.5 出力の正規化（偽陽性を防ぐ工夫）
- JSの表示（`true`/`3`/`Infinity`）とHaskellの `show`（`True`/`3.0`）は綴りが違う。これが差分テストの偽陽性になる。
- 対策：生成Haskellソースに表示ヘルパ **`showJS`**（`3.0`→"3"、`True`→"true"、Infinity/NaN対応）と **`jsMod`**（JSの`%`の符号規約＝truncated division）を埋め込み、`evaluate()`の出力と文字単位で一致させる。
- 数値リテラルは `(n :: Double)` に固定して型推論のdefaulting揺れを防ぐ。

### 8.6 識別子マングリング（name mangling）
- Haskellの識別子は小文字始まりの英数字。ユーザー変数は `v_` 接頭辞＋不正文字の単射エスケープ（例：`数`→`_u6570_`）、合成束縛 `%cond`/`%loop`/`%r` は `phi_` 接頭辞。接頭辞で名前空間が分離されるので衝突しない。

### 8.7 既知の境界
- エラーを含む実行（⊥のforce・反復上限超過）はHaskell側とエラー形式が揃わないので差分テスト対象外。
- 文脈から型が定まらない裸の⊥（`print(z)` 単独）はGHCが曖昧型エラーで拒否（静的検査と動的意味論の境界）。
- 極端な数値（1e21超等）は表示形式がJSと異なる。

### 8.8 IEEE 754
- **IEEE 754**：浮動小数点数の国際標準。`10/0 = Infinity`、`0/0 = NaN` 等の挙動を規定。JSもHaskellのDoubleもこれに従うので、`10/0`が両者で"Infinity"になる。本システムはDiv/Modの0除算をIEEE754ネイティブ挙動に委ねている。

**発表で言えると強い一言**：「Haskellは本システムと同じ遅延評価言語なので、独立した第三者の実装として理想的な検証オラクルになります。生成したHaskellをGHCで型検査し、runghcで実行した出力を自作評価器の出力と突き合わせる差分テストで、変換と評価の両方をまたぐバグを機械的に検出します。ただしトレースはGHCから取れないので、GHCは本番実行系には組み込まずCI専用です。」

**想定質問**：
- Q「なぜ検証しているのは"最終出力"だけ？過程は？」→ A「GHCからは force順序やメモ化回数などのトレースが取れないので、検証水準は"観測等価（コンソール出力列の一致）"に置く。過程（trace）の正しさは自作の evaluator.test.ts が trace を直接アサートして担う。役割分担は"過程＝自前traceテスト、結果＝GHC独立オラクル"。」
- Q「型検査と差分テストの違いは？」→ A「型検査（ghc -fno-code）は実行せず静的に型の整合を見る。差分テスト（runghc）は実際に実行して出力を比較する。前者はif両枝の型不一致等を、後者は最終値の意味論的一致を検証する。」

---

## 第9章 意味保存の検証と3者比較

### 9.1 観測等価（observational equivalence）
- 「2つのプログラムが同じ観測結果（コンソール出力列）を返す」という同値関係。
- 内部ステップの1対1対応ではなく、**外から見える振る舞いの一致**で意味保存を主張する。
- [S]の証明は call-by-value 同士の対称なシミュレーションだが、本システムは「手続型の逐次実行の直感 → call-by-need」という**非対称**な変換なので、内部ステップは1対1にならない。だから観測等価レベルで立てるのが現実的。

### 9.2 シミュレーション定理（simulation theorem）
- 「元プログラムが1ステップ進むと、変換後は複数ステップ進んで対応状態に至る」（`S → S'` ならば `T(S) →* T(S')`）。
- これで「元が値vで停止 ⇔ 変換後も同じvで停止／両方無限ループ」（観測等価）が導ける。

### 9.3 3者比較（本システムの検証の全体像）
```
runProcedural(P)  =  evaluate(transpile(P))  =  runghc(printHaskell(transpile(P)))
   （手続型参照）      （関数型評価器）             （GHC独立オラクル）
   ↑ §4.8            ↑ ③評価器                  ↑ §4.6
```
- **左辺（`runProcedural`, §4.8）**＝手続型ASTを変換せず正格・逐次に直接実行する参照インタプリタ。「①変換元の操作的意味論」の実行可能な定義。
- **中辺**＝本システムの評価器（変換を経由）。
- **右辺**＝生成Haskellの実行。
- この3つが一致することで、**②変換の正しさ（観測等価レベル）**まで機械検証が閉じる。

### 9.4 停止性の非対称（重要な教育ポイント）
- `x=1; while(x>0){x=x+1}; print("hi")` を考える：
  - **手続型（正格・逐次）**：ループが無限に回り、print("hi")に到達しない → 打ち切り。
  - **関数型（call-by-need）**：`x` の最終値に誰も需要を出さないので**ループは一度も走らず**、"hi"を出力して停止。
- これは §4.7 の**非対称シミュレーション**（call-by-need はより多くのプログラムを停止させる）の実例。観測等価が成立するのは「手続型実行が停止するプログラムに限る」。**遅延評価の利点を体感させる好材料**。

**発表で言えると強い一言**：「手続型の参照インタプリタ・自作の関数型評価器・GHCの3つで同じ出力になることを機械検証し、変換の正しさを観測等価レベルで閉じています。しかも"手続型なら無限ループ、関数型なら需要がなく停止"という停止性の非対称が、左右のコンソールに並んで見えます。」

**想定質問**：
- Q「変換が正しいとどうやって保証する？」→ A「3者比較で観測等価を機械検証。加えてtranspiler.test.tsで構造的健全性（SSA名の一意性、φの形、letrecの形）を検証。形式証明ではないが、[S]の4ステップテンプレートに沿っており将来の形式証明への昇格経路も確保してある。」

---

## 第10章 データフローグラフ可視化（Weck & Tichy, DateFlow.md）

### 10.1 採用する視覚語彙
- **ノード種別で外観を区別**：関数/演算（矩形＝`PrimApp`）、値・変数・リテラル（丸／source、入力ポート不要）、動的な関数適用（小さな四角＝apply node）、グラフの入口/出口（黒矢印、sink＝`Do`＝需要の根）。
- **全エッジに型ラベル**（Number/String/Boolean）：論文の中核的貢献。「コード上見えない中間データフローの型を、図の上ですべて可視化」。本システムの型不一致バリデーションの理論的裏付け。
- **レイアウトは上→下**：データの生成順を自然に追える読み順。
- **部分グラフ（subgraph）**：λ式/クロージャを破線枠で描き、外→内エッジで自由変数捕捉を表す（§5.4実装時）。

### 10.2 採用しないもの（意図的な相違）
- 論文の変換経路（λ計算→圏論（Cartesian Closed Category）→unification→reduction）は**採用しない**。本システムは評価器のトレースから直接グラフを導出するので、これらの中間機構が不要。
- 流用するのは**視覚表現層のみ**（ノード/エッジ/型/部分グラフ/apply/source-sink/簡約の発想）。

### 10.3 状態レイヤ（本システム独自の追加）
- ノードは評価状態を可視化：`unevaluated`＝ゴースト/減光、`evaluated`＝強調（メモ化値を表示）。この判定はtraceの読み取りのみ。
- need が届かず未評価のまま残ったノード（短絡された右辺・未消費の無限リストtail等）は減光のまま留まり「計算がスキップされた」ことを直接可視化。
- 演算結果 `true`＝エメラルドグリーン、`false`＝ソフト赤/グレー。

### 10.4 Elision（折りたたみ）と簡約
- **Elision**：force済みかつ入力がリテラルのみ（または埋め込みリテラルを持つ）ノードを1つの結果値ノードに折りたたむ。例：`3 < 5` → `true`。展開ボタンで詳細に戻せる。
- 論文の「reduction（簡約）による中間ノード除去」の発想を流用（意味を保ったまま補助ノードを畳んで origin→target を直結）。unificationアルゴリズム自体は導入しない。

### 10.5 健全性指標（scalability）
- ノード数・エッジ数がブロック数に対し**おおむね線形**に収まることを目安とする。論文の実測（GHC baseパッケージ6036関数、相関係数 r=0.99）が根拠。超線形増加が見えたらノード生成規則の設計ミスを疑う。

### 10.6 ghc-vis との関係
- **ghc-vis（Felsing）**：遅延評価と共有（sharing）の可視化に特化した既存ツール。
- 本システムは Weck & Tichy（データフロー構造）と ghc-vis（遅延評価状態）の中間に位置し、両者を統合する。これが新規性。

**発表で言えると強い一言**：「グラフの視覚語彙は Weck & Tichy のデータフロー図の設計原則を採用しています。特に"全エッジに型ラベルを付ける"のが論文の中核で、コード上見えない中間の型の流れを一望させます。ただし彼らの圏論経由の生成経路は使わず、評価器のトレースから直接グラフを導出します。」

---

## 第11章 手続型参照インタプリタ（§4.8）

### 11.1 役割
- 手続型ASTを、変換を**経由せず**、正格・逐次・可変な変数表で「手続型言語の直感どおり」直接実行する独立実装。`runProcedural(ast)`。
- **目的(a) HCI**：左ペイン下部に「手続型としての実行結果」を表示し、右ペイン（関数型実行）と並べて対比させる。
- **目的(b) 理論**：§4.7テンプレートの「①変換元の操作的意味論」の実行可能な定義。3者比較の左辺。

### 11.2 意味論の対比
| | 手続型参照インタプリタ | 関数型評価器 |
|---|---|---|
| 評価戦略 | 正格・逐次 | call-by-need（遅延） |
| 未束縛変数 | 即時エラーで停止 | ⊥（needが届くまで顕在化しない） |
| 無限ループ | 反復上限で打ち切り（途中出力は保持） | 需要がなければ停止 |
| And/Or | 短絡評価（JS準拠） | 短絡が創発 |
| コード共有 | 評価器と一切共有しない（独立参照） | ― |

- 独立実装にする理由：参照オラクルへの共通バグ混入を防ぐため。

**想定質問**：
- Q「なぜ評価器とコードを共有しない？」→ A「参照オラクルは検証の"正解"側なので、被検証側（評価器）とコードを共有すると共通のバグが両方に入り検出できなくなる。独立実装にすることで初めて相互検証になる。」

---

## 第12章 技術スタック（フロントエンド）

発表で「どう作ったか」を聞かれた時のため。

- **Blockly**：Google製のビジュアルプログラミング（ブロックを組む）ライブラリ。ユーザー入力の基盤。カスタムブロック（`controls_if_ext`, `controls_while_ext`, `logic_compare_ext` 等）を定義。
- **React**：UIライブラリ。コンポーネントで画面を構成。
- **React Flow（reactflow）**：ノードとエッジのグラフを描画するReactライブラリ。⑤の可視化の描画エンジン。カスタムノード（valNode/opNode/ifNode/applyNode/loopNode/printNode）を定義。
- **Zustand**：軽量な状態管理ライブラリ。グラフのノード/エッジ/コンソール出力/折りたたみ状態を保持。
- **TypeScript**：型付きJavaScript。ASTの型（タグ付きユニオン）を厳密に定義。
- **Vite**：ビルドツール・開発サーバ。`npm run dev` / `npm run build`。
- **Vitest**：テストフレームワーク。`npm run test`（通常）/ `npm run test:ghc`（GHCオラクル、`GHC_ORACLE=1`でゲート）。
- **ESLint**：静的解析（lint）。`npm run lint`。
- **GitHub Actions**：CI（継続的インテグレーション）。`.github/workflows/ci.yml` で push/PR時に build→lint→test→test:ghc を自動実行。GHCは haskell-actions/setup で導入。

### 主要ファイル早見表
| ファイル | 役割 |
|---|---|
| `src/compiler/types.ts` | 手続型ASTの型・Value/Thunk/Env等の共通型 |
| `src/compiler/extractor.ts` | ① Blockly→手続型AST |
| `src/compiler/functionalAst.ts` | 関数型ASTの型定義 |
| `src/compiler/transpiler.ts` | ② 手続型AST→関数型AST（SSA変換） |
| `src/compiler/evaluator.ts` | ③ 純粋評価器（call-by-need） |
| `src/compiler/renderGraph.ts` | ⑤ トレース駆動グラフ描画 |
| `src/compiler/haskellPrinter.ts` | GHC検証用Haskell生成（§4.6） |
| `src/compiler/procInterpreter.ts` | 手続型参照インタプリタ（§4.8） |
| `src/compiler/debugPrint.ts` | 開発用コンソール整形 |
| `src/components/BlocklyPane.tsx` | 左ペイン（Blockly＋手続型コンソール） |
| `src/components/FlowPane.tsx` | 右上（React Flowグラフ描画） |
| `src/components/ConsolePanel.tsx` | 左右共通のリサイズ可能コンソール枠 |
| `src/App.tsx` | 全体レイアウト（左右ペイン） |
| `src/store.ts` | Zustand状態管理 |

---

## 第13章 主要な引用文献リスト（そのまま言えるように）

- **Launchbury, J. (1993).** "A Natural Semantics for Lazy Evaluation." POPL. — call-by-need の操作的意味論。本評価器の理論的基礎。
- **Wadsworth, C. (1971).** PhD thesis. — グラフ簡約（共有付き）。sharing の源流。
- **Appel, A. (1998).** "SSA is Functional Programming." — SSAと関数型の対応。φ＝三項演算子/引数の裏付け。
- **Kelsey, R. (1995).** "A Correspondence between CPS and SSA Form." — CPS↔SSA、λ_jump分類、CPS境界。
- **森下大輔 (2013).** 「手続き型プログラムと関数型プログラムの相互変換手法」情報処理学会全国大会. — 直接スタイル変換、自由変数によるパラメータ最小化。
- **住井英二郎・大根田裕一・米澤明憲 (2004).** 「例外処理機構を備えた命令型言語のCPS変換とその定式化」情報処理学会論文誌. — 意味保存の4ステップ証明テンプレート、破壊的代入＋例外の落とし穴。
- **Weck, T. & Tichy, M.** "Visualizing Data-Flows in Functional Programs." Ulm University. — データフロー図の視覚語彙、全エッジ型注釈。
- **Felsing, D.** ghc-vis. — 遅延評価と共有の可視化（関連ツール）。
- **Meyer, A. & Ritchie, D. (1967).** "The complexity of loop programs." — loop言語（原始再帰）vs while言語（チューリング完全）。
- **Kobayashi** — 型ベース不要変数除去（[S]§7が参照、パラメータ最小化の背景）。

---

## 第14章 発表の骨子（3分・10分・質疑の順で）

### 3分版（エレベーターピッチ）
1. 問題：関数型プログラミングは強力だが初学者には抽象的で学習動機が湧きにくい。
2. アイデア：手続型（順次・代入）でブロックを組ませ、裏で関数型に変換して遅延評価で実行し、評価過程をデータフローグラフで可視化する。
3. 効果：手続型のメンタルモデルを入口に、遅延評価・参照透過性・再帰といった関数型の利点を"体験"できる。特に「手続型なら無限ループ、関数型なら需要がなく停止」の対比が遅延評価の本質を見せる。

### 10分版で足すこと
- 5段階パイプライン（評価とUIの完全分離）。
- 核心＝call-by-need（Thunk/WHNF/メモ化/需要の根/短絡の創発）。
- 変換＝SSA、φ、if→三項演算子、while→letrec。
- 検証＝3者比較（手続型参照/評価器/GHC）で観測等価を機械検証、GHCはCI専用オラクル。
- 可視化＝Weck & Tichy の視覚語彙＋遅延評価状態レイヤ（新規性）。

### 質疑で絶対に押さえる5問（本ノートから）
1. なぜ eval() でなく自作評価器か → トレース取得・意味論の明示（第1章）。
2. メモ化は純粋か → benign effect、観測不能（第4章）。
3. 変換の正しさの保証 → 3者比較・観測等価（第9章）。
4. GHCをなぜ本番に入れないか → トレースが取れない・レイテンシ（第8章）。
5. なぜ遅延評価か → 短絡/無限リスト/需要駆動が自然に創発、停止性の利点を体感（第4・9章）。

---

## 付録A 用語ミニ辞典（30秒で言える定義）

- **AST**：プログラムの構造を表す木。
- **SSA**：各変数を一度だけ代入する形。再代入はバージョン番号で別名化。
- **φ関数**：制御フロー合流点で経路依存の値を選ぶ仮想関数。if合流は三項演算子、ループ先頭は関数引数。
- **call-by-value/name/need**：引数を先に評価/使う毎に評価/初回だけ評価しキャッシュ。
- **Thunk**：未評価の計算を包む遅延セル。
- **force**：Thunkを評価して値を取り出す。WHNFまで。
- **WHNF**：最外コンストラクタの殻だけ暴いた形。
- **メモ化/sharing**：force結果のキャッシュ。call-by-need の "need"。
- **需要駆動**：Print（需要の根）から必要なThunkだけforceする。
- **⊥（ボトム）**：非停止/エラー。全型に属す。forceしなければ安全。
- **短絡評価の創発**：and/orを普通の関数にすると遅延から短絡が自然に出る。
- **CPS**：継続を引数で渡すスタイル。非局所制御が要る時に導入。
- **direct style**：普通に値を返すスタイル。本システムの採用。
- **letrec**：自己参照束縛。whileの変換先。
- **Pair/Proj**：対と射影。複数ループ変数の出口値。
- **do記法/脱糖**：Haskellの逐次IO構文と、その基本構文への展開。
- **観測等価**：外から見える出力が一致する同値関係。
- **差分テスト**：2実装の出力を突き合わせる検証。
- **ghc -fno-code**：型検査だけして実行ファイルを作らない。
- **チューリング完全**：一般再帰＋条件分岐で全計算を表現可能。
- **原始再帰 vs 一般再帰**：有界forまで vs while相当（停止しないこともある）。
- **余再帰**：停止しないがWHNF+takeで有限化できる（無限リスト）。
- **benign effect**：観測不能で純粋性を破らない副作用（メモ化）。
- **ブラックホール**：評価中の値を評価中に再要求する循環（⊥）。
</content>
