# Launchbury 対応関係論証（CLAUDE.md §4.7 の確定版）

本文書は、関数型AST（`src/compiler/functionalAst.ts`）の各ノード種別と、
Launchbury "A Natural Semantics for Lazy Evaluation" (1993) の評価規則、
および Haskell の do 記法脱糖規則との対応関係を確定するものである。
CLAUDE.md §4.7 の要求「§5.2（一般再帰）の実装に着手する **前に** 対応関係論証を
一度確定しておくこと」を満たすため、§5.2 実装直前の時点（§5.1 まで実装済み）の
計算断片を基準に確定し、§5.2 で追加する拡張の対応を併せて先に規定する。
`evaluator.ts` ヘッダの対応表はこの文書の要約であり、齟齬がある場合は本文書を正とする。

## 1. 対象計算断片（§5.1 まで）

Launchbury の正規化 λ計算のうち、ユーザー定義ラムダを持たない断片：

| ノード | Launchbury 規則 | 実装（evaluator.ts） | Haskell 脱糖対応 |
|---|---|---|---|
| `Lit` | LIT（拡張。Γ,n ⇓ Γ,n） | `compute: () => expr.value` | リテラル |
| `Var` | VAR（Γ[x↦e],x ⇓ Δ[x↦v],v） | env 経由の Thunk 参照。**ヒープ更新＝メモ化**（force 後に `evaluated` セルへ置換） | 変数 |
| `Let` | LET（ヒープへの割り当てのみ。評価しない） | `run` の Let 分岐：束縛時に force しない | `do{let decls;stmts} = let decls in do{stmts}` |
| `PrimApp` | PRIMOP（拡張規則。引数Thunkを組込関数へ渡す） | `BUILTINS` テーブル。And/Or の非正格性は関数の中身として表現（§3.5） | 組込演算子 |
| `If` | **CASE の Bool 特殊化**（scrutinee のみ WHNF まで評価し、選ばれた分岐の評価に移る） | cond のみ force → 選ばれた分岐のみ force | `if c then a else b = case c of {True→a; False→b}` |
| `Do` | トップレベル評価の起点（需要の根、§3.4） | Print 列をプログラム順に force | `do{e;stmts} = e >> do{stmts}` |

**正規化との対応（重要）**: Launchbury の計算は「適用の引数は変数でなければならない」
という正規形を前提とする（共有の保証のため）。本システムの §5.1 変換が、φが複数の
とき条件式を合成束縛 `%cond_n` として let 束縛し変数参照で共有するのは、この正規化の
直接の対応物である。同じ理由で、§5.2 のループ結果 `%r_n` も let 束縛してから射影する。

**メモ化の benign effect 論証**: VAR 規則のヒープ更新（Γ→Δ）を可変セルで実装するが、
`force()` は同一 Thunk に対し常に同じ値を返す純関数として振る舞い、メモ化の有無は
観測不能である。ゆえに「純粋関数型」の前提を破らない（§3.3）。

**⊥ の扱い**: 未束縛変数の参照は「ヒープに存在しない VAR」であり、Launchbury の
意味論では導出が存在しない（＝⊥）。本実装は force 時に限り制御されたエラーを投げ、
force されなければプログラム全体は停止しうる（§3.6。[S] の適格性述語「全 Var が
束縛済み」を意図的に採用しない根拠、CLAUDE.md §4.7）。

## 2. §5.2 拡張の対応（実装前に確定）

### 2.1 LetRec（自己参照束縛）↔ Launchbury の LET 規則そのもの

Launchbury の LET 規則はもともと **相互再帰的な束縛（letrec）** を扱う
（ヒープに束縛を置いてから本体を評価するため、束縛の右辺が自分自身を参照できる）。
現行実装の `Let` はその非自己参照の特殊化であり、§5.2 の `LetRec` は
「束縛される値が関数（ラムダ＝WHNF）であり、その閉包環境に自分自身を含める」
という形で LET 規則の本来の一般性を回復する。

- 関数値はそれ自体が WHNF（ラムダは値）であるため、`LetRec` は関数値を
  `evaluated` 状態の Thunk として束縛してよい（定義時の無限展開は起きない＝不動点）。
- 現行スコープの制限：`LetRec` が束縛できるのは関数のみ。値レベルの自己参照束縛
  （`xs = cons(1, xs)` 等）は §5.3（余再帰）で導入する。

### 2.2 Apply（飽和適用）↔ APP 規則の制限形

Launchbury の APP 規則（Γ,e x ⇓ Δ,λy.e' → Δ,e'[x/y] ⇓ Θ,v）に対し、本システムは：

- 関数は **名前付き**（`LetRec` で束縛された `%loop_n`）で、適用は常に **飽和**
  （全引数が揃った適用のみ。部分適用なし）。
- 実引数は Thunk として構築し、関数の**定義時環境**（閉包）を仮引数↦実引数Thunkで
  拡張して本体を評価する。これは APP 規則の代入 e'[x/y] を環境で表現したもの。
- 第一級のラムダ値・部分適用・高階関数（汎用 APP 規則）は §5.4 で導入する。
  この制限により、§5.4 実装前の対応関係論証のコストが抑えられる。

### 2.3 Pair / Proj ↔ コンストラクタと CASE（WHNF、§3.2）

複数のループ変数を持つ while の出口値を表現するため、対（cons セルの最小形）を導入する。

- `Pair` の force は **外側のセルだけを暴き、fst/snd の Thunk は未評価のまま返す**。
  これは §3.2「強制は WHNF まで」の最初の具体化であり、§5.3 の無限リスト
  （cons セル + tail Thunk）の直接の準備である。
- `Proj`（fst/snd）は CASE 規則の対応物：scrutinee（対）を WHNF まで force し、
  選択された成分 Thunk の force に移る。
- 射影式（`fst(%r)`, `snd(%r)`）が構文上複製されても、force が到達するのは
  対の中の**同一の成分 Thunk** であるため、共有（§3.3）は保たれる。

### 2.4 ブラックホール検出 ↔ Launchbury のヒープからの束縛除去

Launchbury の VAR 規則は、束縛の評価中その束縛を**ヒープから取り除く**
（Γ[x↦e] の x を除いた Γ で e を評価する）。評価中の x への再需要は導出不能＝⊥
（いわゆるブラックホール）。本実装はこれを Thunk の**過渡状態 `forcing`** で表現する：

- `unevaluated → forcing →（compute 完了）→ evaluated` という遷移とし、
  `forcing` 状態の Thunk への force は制御されたエラー（循環定義＝⊥）を投げる。
- これは §3.1 の「2状態FSM」に対する過渡状態の追加だが、**観測的純粋性は保たれる**：
  `forcing` 状態が観測されるのは意味論上 ⊥ を表すプログラム（真に循環した需要）
  のみであり、停止するプログラムの観測結果には一切影響しない。
- §5.2 の while 由来の letrec は関数束縛のみで値の循環を作らないため、
  現行スコープでこの状態に到達する構成は存在しない（防御的に先行導入する）。
  §5.3 の値レベル自己参照束縛の導入時に本質的になる。

### 2.5 反復上限（CLAUDE.md §5.2(e) の決定事項）

- **決定**: 正格化オプション（`foldl'`/bang pattern 相当）は導入**しない**。
  §3「全値は Thunk」の原則を例外なく維持し、アキュムレータの Thunk 連鎖の
  積み上がりは「計算が need を待って積み上がる」ことの教育的に正確な描写として保持する。
- 代わりに評価器へ **適用回数上限 `MAX_APPLICATIONS`（= 500）** を設け、超過時は
  制御されたエラー（trace 上は error イベント）とする。これは (i) 非停止プログラム
  からのブラウザ保護、(ii) JSコールスタック保護（末尾呼び出し最適化を持たない
  JS 上で force の連鎖はスタックを消費する）、(iii) §4.5 の健全性指標
  （ノード数の線形性）の維持、の3つを同時に担う。
- 上限は意味論の一部ではなく実装リソースの制約であり、上限内で停止する
  プログラムの観測結果には影響しない。

## 3. 意味保存の検証水準（§4.7 テンプレートとの関係）

[S]（住井ら）の4段階テンプレートのうち、本システムが現時点で機械的に検証するのは：

- ③ 適格性述語：「生成された SSA 束縛名に重複がないこと」（`transpiler.test.ts`。
  §5.2 では LetRec の関数名・仮引数名も対象に含める）。
- ④ シミュレーション関係：内部ステップの1対1対応ではなく
  **観測等価（コンソール出力列の一致）** のレベルで立てる。ユニットテストの
  期待値（手続型実行の直感で計算した出力）との一致検証がその実体であり、
  将来 GHC 差分テスト（§4.6）が加われば独立オラクルによる検証へ昇格する。
