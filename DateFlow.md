# データフローグラフ設計原則の抽出（Weck & Tichy 論文より）

出典: Tobias Weck and Matthias Tichy, "Visualizing Data-Flows in Functional Programs", Institute of Software Engineering and Compiler Construction, Ulm University.

本ドキュメントは、上記論文で述べられているデータフロー図（Data-Flow Diagram, 以下 DFG）の設計原則を、本プロジェクト（Blockly → 関数型AST → call-by-need評価トレース → React Flow）へ流用する目的で抽出したものである。図表は本文中でテキスト表現に変換して記載する。

---

## 0. 適合性の前提（重要・最初に読むこと）

論文と本システムはパイプラインが異なる。**採否を明確に区別すること。**

- 論文のパイプライン: Haskellソース → GHC Core（λ計算）→ 圏論（Cartesian Closed Category）表現 → 図生成 → unification → reduction。
- 本システムのパイプライン: 手続型AST → 関数型AST → **call-by-need評価器のトレース** → グラフ描画。

**→ 流用しないもの**: 論文の変換経路そのもの（λ計算→圏→図の3段変換、`fst`/`snd`/`curry`/`uncurry`アローへの分解、Cartesian積による局所スコープ表現、first-order unification による変数解決）。これらは本システムの「トレース駆動描画（§4.5）」という確定済み設計と競合するため採用しない。ただし §7 の「reduction（簡約）による中間ノード除去」の**発想**だけは流用余地がある（後述）。

**→ 流用するもの**: グラフの視覚的表現規則（ノード種別・エッジの意味・型注釈・部分グラフ・apply node・source/sink・簡約による見やすさ確保）。以下の原則群はこの視覚表現層に属する。

各原則の末尾に `[流用: 直接 / 参考 / 不採用]` を付す。

---

## 1. グラフの構成要素（Graph Data Type）

論文はDFGを4要素からなるグラフと定義する。本システムの `renderGraph(ast, trace)` が生成すべき構造の基礎とする。

- **原則1-1 ノード（Nodes）**: ノードは複数の型を持ちうる。ノードの型がその外観と意味を決定する。`[流用: 直接]`
- **原則1-2 部分グラフ（Subgraphs）**: グラフ内のグラフ。破線の枠で囲って区別する。関数（λ式）の内部を表現するのに用いる。`[流用: 直接 — 将来 §5.4 の Lambda 描画に必須]`
- **原則1-3 ポート（Ports）**: ノード上の具体的な接続点。多くのノードは複数の入力ポートと1つの出力ポートを持つ。各ポートにはデータフローの型が対応づく。`[流用: 直接]`
- **原則1-4 エッジ（Edges）**: 有向で、ちょうど2つのポートを接続する。接続される2ポートは**同一の型（正確には同一のデータフローそのもの）を参照しなければならない**。`[流用: 直接]`

---

## 2. ノードの視覚的表現規則（Basic Constructs）

論文の Figure 1 / Figure 6 に相当。各構文要素の描画規則をテキストで記述する。

### 2-1. 関数ノード（矩形）
- 矩形ノードは関数（その入力を含む）を表す。関数名でラベル付けする。1つ以上の入力フィールドを持ちうる。`[流用: 直接 — 本システムの opNode/PrimApp に対応]`
- テキスト表現:
  ```
  入力(上から): x :: a
        ┌───────────┐
        │   f       │   ← 関数名でラベル
        └───────────┘
  出力(下へ):   y :: b
  ```

### 2-2. 定数/値ノード（丸／ソースノード）
- 丸いノードは入力を持たない定数データフローの源（source）を表す。常に同じ値を返す。変数（`Var`）とリテラル（`Lit`）は各々1つのノードへ翻訳され、出力ポートがその値となる。入力ポートは不要（計算に入力を要さないため）。`[流用: 直接 — 本システムの valNode に対応]`
- テキスト表現:
  ```
  ( 5 )        ( x )
     │ Int        │ Int
     ▼            ▼
  ```

### 2-3. Source ノードと Sink ノード（グラフ全体の入口・出口）
- 小さな黒い矢印型ノードが、データフロー図全体の source（入口）と sink（出口）になる。start ノード（source）が可視化対象関数の入力、end ノード（sink）がその出力を表す。`[流用: 直接 — グラフの根/末端の明示。本システムでは §3.4 の「需要の根＝Doノード」を sink 側に対応づけると意味論的に整合する]`
- テキスト表現:
  ```
   ▼   ← start node (source, 入力)
   …
   ▲   ← end node (sink, 出力)
  ```

### 2-4. Identity ノード（恒等ノード）
- データフローが変化せず素通りするだけの特殊ノード。論文ではアルゴリズムが常に2ポートを返す必要から導入される。`[流用: 参考 — 本システムはトレース駆動でアルゴリズム上の都合が異なるため必須ではない。§7の簡約で消せる中間ノードの一種として認識しておく]`

### 2-5. Apply ノード（動的に適用される関数）
- **静的な定義を持たない関数（＝動的な値としての関数）**が適用される箇所を表す小さな四角のノード。左から入ってくるデータフローは「正しい型の関数」でなければならない。上から入ってくるデータフローにその関数を適用する。`[流用: 直接 — 本システムの将来 §5.4 `Apply`(ユーザー関数) ノードの描画規則そのもの]`
- 通常の関数ノード（2-1、静的定義あり）と apply ノード（動的な関数値の適用）を**視覚的に区別する**こと。
- テキスト表現:
  ```
  関数値 ─▶┌─┐
           │a│  ← apply node (小さい四角)
           └─┘
            │
        引数 x を上から受けて f(x) を出力
  ```

---

## 3. 型注釈の原則（Type Enrichment）— 本論文の中核的貢献

論文が最も価値を主張する点。あなたのシステムの型可視化要件（Boolean専用形状・型不一致フィードバック）と直結する。

- **原則3-1 全中間データフローへの型付与**: すべてのエッジ（データフロー）に型を明示的にラベル付けする。コード上は見えない中間処理ステップの型を、図の上では**すべて可視化**する。これが「型がプログラマに通常見えない」問題への解決策である。`[流用: 直接 — 本システムの Number/String/Boolean 型可視化と完全に一致。エッジに型ラベルを付す設計を採用する]`
- **原則3-2 型がプログラム理解の主要な導き**: 「あるノードにどの型の値が流れ込み・流れ出るか」を見せることで、関数の役割理解を助ける。型はノード間の適合性（どの関数がどのデータを処理できるか）を示す。`[流用: 直接 — 学習者に型の流れを体験させる本プロジェクトの教育目標と一致]`
- **原則3-3 型による接続妥当性の保証**: エッジで接続される2ポートは同型でなければならない（原則1-4の再掲）。これは本システムの「同一型のみ接続許可（Type Mismatch防止）」バリデーションの理論的裏付けになる。`[流用: 直接]`

---

## 4. データフローの読み方向とレイアウト原則

- **原則4-1 上から下への流れ**: データフローはグラフの上から下へ流れる。論文は「これがプログラムコード（パラメータが各所に散在する）より自然な読み順を与える」と主張する。`[流用: 直接 — React Flow のレイアウト方向を上→下（top-to-bottom）に統一する根拠]`
- **原則4-2 部分グラフによる closure の表現**: λ式（関数値）は破線枠の部分グラフとして描く。外側グラフと部分グラフの間をエッジが接続することで、closure（自由変数の捕捉）を可視化できる。`[流用: 直接 — 将来 §5.4 のラムダ/クロージャ描画。部分グラフの外から中へ伸びるエッジ＝捕捉された変数、という規則を採用する]`
- **原則4-3 カリー化の入れ子表現**: 複数引数関数は「関数を返す関数」の入れ子であり、各内側関数が新しい source と sink を導入する入れ子部分グラフとして描かれる。`[流用: 参考 — 本システムの「部分適用を見据えた内部データ構造」（既存指示書 §4.5）と発想が一致。ただし初学者向けには視覚ノイズになりうるので、Elision/折りたたみと併用する]`

---

## 5. スケーラビリティと簡約（Reduction）の原則

論文は「素朴に生成するとグラフが pattern node / identity node で膨張し見づらくなる」問題を認識し、簡約段を設ける。

- **原則5-1 中間・補助ノードの除去による簡約**: 純粋な接続点にすぎないノード（identity ノード、pattern ノード）は origin から target へ直接エッジを引き直すことで除去できる。データフローは「その値がどこで生まれたか（start node / 関数ノードの出力 / 値ノードの出力 / apply ノードの出力 / 部分グラフ）」まで後ろ向きに辿れる。`[流用: 参考 — 本システムはトレース駆動なので論文の unification は不要だが、「見やすさのために意味を保ったまま補助ノードを畳んで origin→target を直結する」という発想は、本システムの Elision（折りたたみ）とレイアウト整理に直接応用できる]`
- **原則5-2 グラフサイズの線形性**: 論文の実測（GHC baseパッケージ6036関数）で、ノード数・エッジ数はソースコードサイズに対し線形（相関係数 r=0.99、傾き約1.82）に収まる。「各値は単一ノード＋単一エッジを導入し、一部の関数適用が apply ノードと追加エッジを1つ導入するにとどまる」ため。`[流用: 参考 — 本システムでもノード数がブロック数に対し線形に収まることを健全性の目安にできる。爆発的増加が起きたら設計ミスを疑う指標として使う]`
- **原則5-3 「全体像を見せる」ことの価値**: 論文は「パズルを解くとき、個々のピースを順に試すより全体を見たほうが、どこにピースが嵌まるか分かりやすい」という比喩で、データフロー全体と型を一望できることの認知的利点を主張する。`[流用: 参考 — 本プロジェクトの「手続型を裏で関数型として一望させる」教育目標の裏付けとして引用できる]`

---

## 6. 遅延評価の可視化に関する示唆（Related Work より）

- **原則6-1 遅延評価の可視化は別ツールの主題**: 論文は関連研究として ghc-vis（Felsing）に言及し、それが「遅延評価と共有（sharing）の可視化」に特化する一方、本論文自身は**より高レベルのプログラム理解**を目的とし遅延評価そのものの可視化は狙っていない、と明確に区別している。`[流用: 参考 — 重要な位置づけの示唆]`
  - **本システムへの含意**: あなたのシステムは「データフロー構造の可視化（本論文の領域）」と「call-by-need評価過程の可視化（ghc-visの領域）」の**両方を統合しようとしている**点で、両者の中間に位置する。論文の視覚語彙（ノード/エッジ/型/部分グラフ）を骨格に使いつつ、そこへ「force済み/未評価（ゴースト）」というghc-vis的な状態レイヤ（本システム §4.5）を重ねる、という統合方針を明示的に自覚しておくこと。これは既存いずれの研究とも異なる本システムの新規性の所在になりうる。

---

## 7. 本システムへ採用する設計原則の要約（実装チェックリスト）

以下を `renderGraph(ast, trace)`（§4.5）およびノード描画コンポーネントの実装指針とする。

- [ ] エッジは有向・2ポート接続・同型制約を満たす（原則1-4 / 3-3）。
- [ ] ノード種別で外観を変える：関数/演算（矩形）、値・変数（丸／source）、動的関数適用（apply, 小四角）、グラフ全体の入口(start)・出口(sink)（黒矢印）を区別する（原則2-1〜2-5）。
- [ ] **全エッジに型ラベルを付す**（原則3-1）。これは論文中核かつ本プロジェクトの型可視化要件と直結。
- [ ] レイアウトは上→下の流れに統一する（原則4-1）。
- [ ] λ/クロージャは破線枠の部分グラフで描き、外→内エッジで自由変数捕捉を表す（原則1-2 / 4-2）。将来 §5.4 実装時に適用。
- [ ] 見やすさのため、意味を保ったまま補助ノードを畳み origin→target を直結する簡約発想を、Elision/レイアウト整理に流用する（原則5-1）。
- [ ] ノード数がブロック数に対し線形に収まることを健全性指標とする（原則5-2）。
- [ ] apply ノード（動的関数値の適用）と通常の関数ノード（静的定義）を視覚的に区別する（原則2-5）。将来 §5.4 実装時に適用。

## 8. 本システムで論文と意図的に相違させる点（混同防止）

- 論文の圏論経由変換（λ→CCC→図）・first-order unification・pattern node 機構は**採用しない**。本システムは評価器のトレースからグラフを導出する（§4.5）ため、これらの中間機構を必要としない。
- 論文は「関数型ソースを入力」とするが、本システムは「関数型ASTを入力」とし、さらにその手前に手続型ブロックがある。source/sink や部分グラフの視覚規則は流用するが、生成経路は流用しない。
- 論文は遅延評価の可視化を範囲外とするが、本システムはそれを中核要件とする（原則6-1 の含意）。論文の視覚語彙に「force済み/未評価ゴースト」状態レイヤを追加する形で拡張する。