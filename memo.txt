# 追加・変更ブロックおよび機能メモ

本環境に実装されているカスタムブロックおよびトランスパイラ/可視化機能の要約です。新しいブロックや機能の追加・変更を行った際は、このメモを更新してください。

---

## -1. 実行パイプラインの5段階分離（関数型ASTの独立・純粋評価器・トレース駆動グラフ描画）

`CLAUDE.md` §4 の規定に基づき、従来「グラフ構築」と「評価」が `transpiler.ts` 1ファイルに融合していたアーキテクチャを、以下の5段階パイプラインへ全面移行しました。対象は既存4カテゴリ（変数／数学・計算／論理・比較／テキスト出力）ブロックのみで、新規ブロックの追加は行っていません。

### 新設・変更ファイル
- `src/compiler/functionalAst.ts`（新設）：独立した関数型ASTの型定義（`Lit`/`Var`/`Let`/`PrimApp`/`Do`）。React Flow・Zustandに一切依存しない。
- `src/compiler/transpiler.ts`：役割を「①手続型AST→②関数型ASTへの変換」のみに縮小（`transpileToFunctionalAst`。旧`transpileToSSA`は廃止）。評価ロジック・グラフ構築ロジックは一切持たない。
- `src/compiler/evaluator.ts`（新設）：③純粋評価器。`evaluate(program): { trace, consoleOutput }` というUI非依存の純粋関数。Thunk/force/メモ化/BUILTINSは`transpiler.ts`から移設し、UIノードへの直接書き込みを完全に廃止（forceイベントは`trace`配列への push のみ）。
- `src/compiler/renderGraph.ts`（新設）：⑤トレース駆動グラフ描画。`renderGraph(program, trace): { nodes, edges }` という、評価を一切行わない純粋関数。
- `src/components/BlocklyPane.tsx`：`handleRun`が新しい4段階呼び出し（`transpileToFunctionalAst`→`evaluate`→`renderGraph`→`updateGraph`）に変更。
- `src/components/FlowPane.tsx`：ノードのHandle位置を左右（Left/Right）から上下（Top/Bottom）へ変更（後述のレイアウト方向転換に対応）。

### 視覚設計原則の採用（Weck & Tichy論文 / `DateFlow.md`）
- **レイアウトを上→下に統一**（従来は左→右）。`renderGraph.ts`内のレイアウト計算は、深さ＝Y・兄弟の広がり＝Xという軸の入れ替えにより実現。
- **全エッジに静的型ラベル（Number/String/Boolean）を付与**。演算子の型はforceを伴わない静的推論（PrimAppの返り値型はopから一意に決まる、Litはtypeofから、Varは束縛元を再帰的に辿る）で決定するため、未評価（ゴースト）ノードへ向かうエッジにも型ラベルが表示される。

### 既知の変更点・制限
- 全Print文は、元のソースコード上の記述位置に関わらず、実行パイプライン上は単一の終端`Do`ノードへ集約される（評価順序はプログラム順のまま維持されるが、グラフ上の水平位置は「全Let列→全Print列」の順で配置される）。
- `CLAUDE.md` §4.6（`haskellPrinter.ts` + GHC差分テストによるCI検証オラクル）は、開発環境に`ghc`/`runghc`が存在せず動作検証ができないため、今回は見送りました。次回以降の課題です。
- テストフレームワークとして `vitest` を新規導入し（`npm run test`）、評価器・グラフ描画それぞれについて単体テストを追加しました（`src/compiler/evaluator.test.ts` / `src/compiler/renderGraph.test.ts`）。ビルド・リンター・自動テストは全て合格を確認済みですが、この開発環境にはブラウザ自動操作ツールが無く、実際のBlockly UIでのクリック操作・画面表示の目視確認は未実施です。

### 「変数」ツールボックスカテゴリの修正（複数変数対応）
- Blockly標準の動的カテゴリ（`custom: 'VARIABLE'`）は、`variable_blocks`スタイルとして登録されている標準ブロック`math_change`（「xを1増やす」相当）を自動的にフライアウトへ混入させてしまうため、一時的に`variables_set`/`variables_get`のみの静的カテゴリへ置き換えていた。しかしこの静的カテゴリには「新しい変数を作成」の導線が無く、ドラッグされる全ブロックがBlocklyの単一デフォルト変数を共有してしまい、いずれかのブロックで変数名を変更すると他の全ブロックの変数名も連動して変わる（＝複数の独立した変数を作成できない）という不具合が発生した。
- `workspace.registerToolboxCategoryCallback('CUSTOM_VARIABLE', ...)` を用い、`Blockly.Variables.flyoutCategory(ws, false)`（標準の動的フライアウト生成結果：変数作成ボタン・変数ごとのget/setブロックを含む）から`type === 'math_change'`のブロックだけを除外する方式に変更した。これにより「複数変数の作成・個別の名前変更」という標準機能を維持したまま、不要な`math_change`ブロックの混入のみを防いでいる。

---

## 0. 評価モデルの全面刷新（call-by-need / 真の遅延評価）

`CLAUDE.md` の規定に基づき、評価モデルを Launchbury の自然意味論に基づく **call-by-need**（真の遅延評価）へ全面刷新しました。対象は既存の4カテゴリ（変数／数学・計算／論理・比較／テキスト出力）ブロックのみで、新規ブロックの追加は行っていません。

### 演算子の第一級関数適用への統一
- 算術演算（`Add`/`Sub`/`Mul`/`Div`/`Pow`/`Mod`）と比較・論理演算を、AST上で単一の `ApplyNode` に統合しました（従来は `ArithmeticNode` と `ApplyNode` に分裂していました）。すべての演算子は「特殊構文」ではなく「第一級関数適用」として一様に扱われます。

### Thunkベースの2パス評価（トランスパイラの中核刷新）
- `src/compiler/transpiler.ts` を、**Pass 1（構造構築）** と **Pass 2（需要駆動評価）** の2段階に分離しました。
  - Pass 1: ASTを走査し、短絡評価で読まれない可能性がある部分も含めて **すべての部分式を無条件にノード/エッジ化** します。この時点では一切の値計算は行いません（全ノードは `unevaluated`）。
  - Pass 2: 仮想コンソールへの出力（`Print`文）を **需要の根（root of the demand spine）** とし、そこから `force()` を辿って実際に必要な部分だけを評価します。需要が届かなかったノード（短絡された右辺、印字も他の計算にも使われない変数など）は最後まで `unevaluated` のまま残ります。
- **短絡評価は特殊分岐として実装されていません。** `And`/`Or` は他の演算子と同じ `BUILTINS` テーブル上の通常の（非正格な）組込関数として定義されており（例: `Or: (a) => force(a[0]) ? true : force(a[1])`）、右辺が force されないという挙動は遅延評価から自然に創発します。
- Thunkは「未評価（計算を再現するクロージャを保持）」「評価済み（値をメモ化保持）」の2状態FSMとして `types.ts` に定義され（`Thunk`/`Env`型）、一度forceされた値は再計算されずキャッシュされます（メモ化はコード上「observationally pureなbenign effect」として明記）。

### 未評価（ゴースト）状態の可視化と仕様変更
- ノードは `data.evalState`（`'unevaluated' | 'evaluated'`）を持ちます。未評価ノードは減光表示（ゴースト）され、構造は保ったまま結果を表示しません。
- **【仕様変更】** 従来、And/Orの短絡評価時には右辺のノード自体が生成されず非表示でしたが、刷新後は右辺の部分式（ノード・エッジ）は必ずグラフ上に生成され、force されなかった場合はゴースト（減光・展開状態）としてそのまま可視化されます。「計算がスキップされた」ことが、ノードの消失ではなく明示的な未評価状態として観測できるようになりました。
- 評価済みの演算子ノードには、forceされなかった入力（該当があれば）を示す汎用バッジ「need未到達の入力あり」を表示します（従来のAnd/Or専用の「短絡評価」バッジを置き換え）。
- エッジは、供給元ノードが評価済みの場合のみアニメーション表示され、ゴーストへ向かうエッジは破線・減光表示になります。

### 未束縛変数と0除算の健全な扱い
- 未割り当ての変数を参照した場合、クラッシュせず「未束縛」ゴーストノード（⊥）としてグラフ化されます。実際にforceされた場合のみ制御されたエラーとして扱われ、該当する `Print` 文はコンソールにエラー行を出力しつつ、後続の `Print` 文の処理は継続されます（1箇所の需要失敗がシステム全体をクラッシュさせません）。
- 0除算・0剰余は、黙って0を返す特殊処理を撤去し、IEEE754のネイティブ挙動（`Infinity`/`-Infinity`/`NaN`）に委ねるようにしました。

### Elision（折りたたみ簡約表示）の再定義
- 折りたたみ（Elision）は「force済み（評価済み）かつメモ化されたThunkで、入力がリテラルのみ、または埋め込みリテラルを持つノード」として再定義されました。未評価のノードは折りたたまれず、常に展開されたゴースト状態で表示されます。

### textブロックの復元
- 文字列リテラルブロック（`text`）が「テキスト出力」カテゴリのツールボックスに存在せず作成不可能だったため復元しました。`extractor.ts` 側は既に対応済みでしたが未接続だったため、`logic_compare_ext` のString型比較機能が実質的に到達不能でした。

### 既知の制限
- `variables_get` ブロックは型無しの動的変数であるため、比較ブロックの型バリデーション（`logic_compare_ext` の `onchange` フック）は変数同士の比較をすり抜けます（リテラル絡みの比較のみ検出可能）。変数の静的型推論は未実装で、今後の課題です。
- 折りたたみ状態（`foldedNodeIds`）のセッションをまたいだ永続化、および需要・値伝播のタイムライン的なアニメーション演出は今回のスコープ外です。

---

## 1. 数学・計算 / 変数関連の拡張
### 複合代入ブロック (`math_change_ext`)
- **ブロック機能**: 指定した変数に算術演算（`+=`, `-=`, `*=`, `/=`, `^=`, `%=`）を施し、その結果で変数自体を更新（再代入）します。
- **データフロー変換**: 変数の再代入が発生した際、静的単一代入（SSA）の考え方に則って自動的に「新しいバージョン（例: `x_2`）」の変数ノードを作成し、計算ノードからエッジを接続します。

### 剰余演算（%）の追加
- **ブロック機能**: 標準の算術演算ブロック (`math_arithmetic`) の選択肢に、剰余演算（`%`, `MODULO`）を追加しました。

---

## 2. 論理・比較演算子ブロック
Boolean を扱う演算子群です。型制約が厳密に定義されています。

### 真偽値ブロック (`logic_boolean_ext`)
- **ブロック機能**: `true` または `false` の真偽値リテラルを出力するブロックです。
- **コネクタ型**: 出力 `Boolean`

### 比較ブロック (`logic_compare_ext`)
- **ブロック機能**: 2つの入力値（数値または文字列）の大小関係や一致判定を行います（`=`, `≠`, `<`, `≤`, `>`, `≥`）。
- **コネクタ型**: 入力 A, B（`Number` または `String`）、出力 `Boolean`
- **バリデーション機能**: 左右に入力されたブロックの型が一致していない場合（例: 数値と文字列を比較しようとした場合）、警告が表示され自動的に接続が解除（切断）されます。

### 論理演算ブロック (`logic_operation_ext`)
- **ブロック機能**: 2つの真偽値入力の論理積（`AND`）または論理和（`OR`）を計算します。
- **コネクタ型**: 入力 A, B（`Boolean`）、出力 `Boolean`

### 否定ブロック (`logic_negate_ext`)
- **ブロック機能**: 入力された真偽値を反転（`NOT`）させます。
- **コネクタ型**: 入力（`Boolean`）、出力 `Boolean`

---

## 3. トランスパイラと React Flow 可視化機能
手続型のブロックのつながりを、純粋関数型のデータフローに可視化・変換する際の独自機能です。

### 演算子の純粋関数適用化
- 比較・論理演算は内部で `Apply(Operator, [Args...])` の純粋な関数適用に変換して処理します。これにより、将来的な高階関数（部分適用など）への引き渡しが可能になっています。

### エッジの左右（左辺・右辺）の視覚的区別（マルチポート化）
- 算術演算や論理・比較演算（二項演算）は、左辺と右辺の順序が意味を持つため、演算ノード（`opNode`）の入力ターゲットピンを2つに分割（上側の `left` ピンと下側の `right` ピン）しました。
- 各ノードの左端に小さく `L`（左辺）と `R`（右辺）の視覚的ラベルを表示し、それぞれの入力ピンに正確にエッジが接続（`targetHandle: 'left' / 'right'`）されるようにすることで、グラフ上のどこから計算が入力されているかを一目で判別できるように改善しました。

### 短絡評価（遅延評価）
- `AND` / `OR` のトランスパイル時、左辺の結果から全体の判定が確定する場合（例: `false AND ...`）、右辺の評価を完全にスキップ（Thunk化）します。これにより、右辺に未定義の変数が存在していてもエラー（クラッシュ）が発生しません。

### Elision（折りたたみ簡約表示）
- `3 < 5` や `true AND false` など、入力がすべてリテラル値の自明な演算については、初期描画時に1つの結果値ノード（例: `3 < 5 → true`）に折りたたんで表示されます。
- ノード上の「**展開 ▽**」ボタンを押すと、隠れていた入力リテラルノード群とエッジが視覚的に展開され、詳細な評価過程を確認できます。

### 部分適用・リテラルの埋め込み
- 変数と比較する演算（例: `x < 5`）の場合、リテラル値 `5` は演算ノードのラベルに埋め込まれ、`< 5` という単一ノードとして表示されます（変数 `x_1` からこのノードへ1本のエッジだけが繋がります）。
- 演算ノードの「**展開 ▽**」ボタンを押すと、埋め込まれていたリテラル `5` を外側のノードとして展開し、エッジが接続された状態を表示できます。

### 階層的レイアウト（Tree Layout）の自動計算
- `(x < 5) AND (x = 5)` のように演算子がネストして呼び出される場合、ノード同士が重なって視覚的に見えづらくなるのを防ぐため、構文木の深さ（ネストレベル）とスパン情報に基づいて各ノードの X、Y 座標を自動的に階層配置するレイアウトアルゴリズムを導入しました。データフローは常に左（葉ノード・入力値）から右（親の式・出力先）へと規則正しく流れるように配置されます。

### ノードの動的サイズ調整と縦幅削減
- `ValNode`（値/変数用）および `OpNode`（演算用）の上下パディングを `2px` (左右 `8px`)、最小高さを `20px` に抑え、境界線を `1.5px` に細くすることで、縦幅を大幅に短縮し極めてスリムな外観に調整しました。
- 従来はデフォルトの React Flow ノード（固定幅 150px）で表示されていた `Print` ノードを、専用のカスタムコンポーネント `PrintNode` として新しく定義・登録しました。これにより、他のノードと同様に縦幅を極限までスリム化し、`width: 'max-content'` と `whiteSpace: 'nowrap'` を適用して、出力された値の長さに合わせて横幅が動的にぴったりとフィットする極小レイアウトを実現しました。
- 全ノードのフォントサイズを `11px` （内部バッジは `8px`）に調整し、`gap: '4px'` でレイアウトを最適化しました。
- 各ノードに `width: 'max-content'` および `whiteSpace: 'nowrap'` を適用したことで、「VALUE」や「VAR」、「PRINT」などのラベル文字が隠れず見える最小幅を維持しつつ、値の桁数や長さが増えた時には自動的に横幅が追従して伸縮するレスポンシブなUIを実現しました。

### ノード選択時の動的ハイライトとエッジ可視化
- グラフ上の任意のノードをクリックした際、そのノードの境界線を鮮やかなオレンジ（`#ff5722`）で強調し、周囲にソフトな光彩（`boxShadow`）を施すアクティブエフェクトを実装しました。
- React Flow標準の `onNodesChange` / `onEdgesChange` をZustandストア側のアクションとして統合しました。これにより、他のノードから別のノードへ直接クリック移動した際やドラッグ時の選択状態が状態管理（Zustand）に即座かつ100%正しく書き込まれ、ノード境界の強調とエッジのハイライト（太いオレンジ実線＋アニメーション実行）が一切の遅延や競合なく完全に連動して変化するロバストな仕組みを実現しました。
- 選択されたノードと無関係なエッジは半透明の薄いグレー（`opacity: 0.3`）に減衰（フェードアウト）し、複雑なデータフローグラフであっても、選択した要素の依存関係が瞬時に識別可能です。背景（空スペース）をクリックすると、選択がクリアされ通常状態に戻ります。

## §5.1 if-else（条件分岐）ブロックの実装

### `controls_if_ext` ブロック（新カテゴリ「制御」）
- 「もし %COND ならば %THEN そうでなければ %ELSE」という固定形（elseif無し）のif-else文ブロックを追加しました。COND入力はBoolean型チェック付き、THEN/ELSEは文スタック（statement input）です。分岐本体には代入系ブロック（`variables_set` / `math_change_ext`）と入れ子の `controls_if_ext` を置けます。
- **分岐内の `text_print`（表示）は現行スコープ未対応**です。需要の根が条件付きになり、無条件のPrint列である `Do` ノードの意味論（CLAUDE.md §3.4）を超えるためで、変換時に明示エラーを投げ、仮想コンソールに「[変換エラー] ...」として表示されます（`BlocklyPane.tsx` の `handleRun` に try/catch を追加）。

### 変換規則（transpiler.ts、CLAUDE.md §5.1「φ＝三項演算子の返り値」）
- SSA版数管理を「単調増加カウンタ（`versionCounter`、決して巻き戻さない＝束縛名の一意性を構造的に保証）」と「現在参照すべき版（`current`、分岐で独立に進行し合流点で切替）」の2層に分離しました。
- 分岐内で代入された各変数 v に対し、合流点でφ束縛 `let v_new = If(cond, v_then版, v_else版)` を生成します。片側でしか代入されなかった変数の反対側の腕は分岐前の版（未代入なら版0＝未束縛⊥）を参照します。
- 分岐内の束縛は合流点より前へ「巻き上げ」て平坦なLet連鎖に並べます。call-by-needでは束縛＝評価ではない（LaunchburyのLET規則はヒープ割り当てのみ）ため、選ばれなかった分岐の束縛には需要が届かず未評価のまま残る、という健全性の裏付けがあります（正格言語では不健全な変換）。
- 条件式はφが複数あるとき合成束縛 `%cond_n` として一度だけlet束縛し、各φから変数参照で共有します（複製するとsharing違反＋グラフノード重複。Launchburyの正規化「適用の引数は変数」に対応）。φが1つなら直接埋め込み、条件がもともと変数参照なら既存束縛を参照します。

### 関数型AST・評価器（functionalAst.ts / evaluator.ts）
- `FIf`（`kind:'If'`、cond/then/else）ノードを追加しました。評価規則は「scrutinee（条件）のみWHNFまでforceし、選ばれた分岐だけをforceする」＝And/Or（§3.5）と同様に非正格性が遅延評価から創発する形で、特殊な簡約規則はありません。Launchbury対応: CASE規則のBool特殊化 ↔ Haskellの脱糖 `if c then a else b = case c of {True->a; False->b}`（evaluator.tsヘッダの対応表に追記済み）。

### 可視化（renderGraph.ts / FlowPane.tsx）
- `ifNode`（新ノード型）: cond/then/elseの3入力ハンドルを持つ矩形ノード。評価済みのifノードには条件の値から判定した「then側スキップ / else側スキップ」バッジを表示します。選ばれなかった分岐の腕・巻き上げられた束縛は既存のゴースト減光・破線エッジ規則だけで自動的に「スキップされた計算」として可視化されます。
- `%cond` 合成束縛は紫系の「条件」ノード（`isCondBinding`）として描画し、ユーザー変数の「最新版黄色ハイライト」の対象から除外しています。
- φの型推論: 両腕の型が一致すればその型、片腕が未束縛（⊥）ならもう一方の型を採用します（⊥は全型に属する、§3.6）。

### テスト（24件、全パス）
- transpiler: diamond合流のφ生成／%cond共有と片側代入のy_0（⊥）腕／入れ子if-elseでの束縛名一意性／分岐内Printの明示エラー／空分岐if-elseの完全消滅（需要駆動の帰結）。
- evaluator: 選ばれた分岐のみforce（else側束縛はtrace上にイベント無し）／選ばれなかった分岐内の未束縛変数が⊥のまま触られずクラッシュしないこと。
- renderGraph: ifNodeの3本の型ラベル付きエッジとskippedBranchバッジ／%cond条件ノードが単一で両φに接続されること。
- ブラウザ上での手動UI確認は未実施です（ユーザーによる確認待ち）。

## §5.2 while（くり返し）ブロックの実装

### 前提として確定した2点（実装着手前）
- **Launchbury対応関係論証**: `launchbury.md` として確定（§5.1までの対応表、§5.2拡張＝LetRec/制限付きAPP/Pair(WHNF)/ブラックホール検出、%cond・%r束縛と正規化「適用の引数は変数」の対応、観測等価による検証水準）。evaluator.tsヘッダの対応表は同文書の要約。
- **アキュムレータThunk連鎖への方針（CLAUDE.md §5.2(e)）**: 正格化オプションは導入せず（§3の原則を例外なく維持）、評価器に適用回数上限 `MAX_APPLICATIONS`（=500）を設けた。超過時は制御されたエラー（traceのerrorイベント→仮想コンソールに表示）。

### `controls_while_ext` ブロック（「制御」カテゴリ）
- 「%COND のあいだ %DO をくり返す」ブロックを追加。COND入力はBoolean型チェック付き。
- **現行スコープの制限**（変換時に明示エラー）: ループ内のPrint（需要の根が条件付きになるため。if分岐内と同じ理由）、ループの入れ子（式レベルletrecが必要になるため次段階）。if分岐の中のwhileは束縛の巻き上げによりプログラムレベルに現れるため対応済み。

### 変換規則（transpiler.ts、transform.md §1 (a)〜(c)）
- ループ本体＝letrecの自己参照関数 `%loop_n`、ジャンプ＝末尾自己呼び出し。
- ループ内で再代入される変数＝関数の仮引数（**ループ先頭のφ**。初回実引数＝突入前の版、末尾呼び出し実引数＝周回末尾の版）。再代入されないループ不変変数＝仮引数にせず外側スコープの自由変数として閉包に捕捉。
- 本体内のSSA束縛は反復ごとに評価されるため式レベルの `LetIn` とし、then腕の内側に畳む。条件は仮引数の版で本体実行前に判定。
- 複数ループ変数の出口値は入れ子の `Pair` で返し、適用結果 `%r_n` を一度だけlet束縛してから `Proj` で射影（射影式が複製されても対の中の同一Thunkを共有するため再計算なし）。ループ変数が1つなら対を作らず直接束縛。

### 関数型AST・評価器
- 追加ノード: `FLetIn`（式レベルlet）/ `FLetRec`（関数のみの自己参照束縛）/ `FApply`（名前付き関数の飽和適用）/ `FPair`・`FProj`（対と射影。WHNF＝外側のみ暴き成分Thunkは未評価のまま）。
- Value型を `PairValue` / `FnValue` で拡張し、表示は `showValue`（対・関数はforceせずWHNFのまま表示）に統一。
- Thunkに過渡状態 `forcing` を追加（**ブラックホール検出**）。forcing中のThunkへの再need＝循環定義（⊥）を制御されたエラーにする。LaunchburyのVAR規則の「評価中の束縛のヒープからの除去」に対応。現行の変換では到達する構成が存在しない（防御的導入。§5.3の値レベル自己参照で本質的になる）。
- 需要駆動の帰結: 結果に需要が無いループ（`while(true){...}; print("hi")`）は一度も走らずプログラムは停止する（テストで確認）。

### 可視化（renderGraph.ts / FlowPane.tsx）
- 束縛解決を binderIndex（プログラムLet・LetRec関数名・仮引数・式レベルLetInの全束縛の索引）に一般化。
- 新ノード: `loopNode`（破線枠＝静的な関数定義、§4.5の視覚語彙）、`applyNode`（小さな四角＝動的適用、§4.5）。関数定義→applyへ破線エッジを引き、末尾自己呼び出しではこれがグラフ上のサイクル＝再帰の表現になる。仮引数ノードは上段に並べ、ループ不変変数への参照は自由変数捕捉エッジとして自動的に外側の束縛ノードへ接続される（Weck & Tichy 原則4-2）。
- ループ本体は静的に1回だけ描き、trace上の同一nodeIdの複数force（反復ごとの別Thunk）は最初の発生＝**初回反復の値**を表示する。反復ごとの展開表示（Thunk連鎖の可視化）は次段階の拡張として保留。
- 対・射影は既存のopNode（`⟨ , ⟩` / fst / snd）として描画。OpNodeの単項判定を `args.right` の有無に一般化。

### テスト（34件、全パス）
- transpiler: ループ変数→仮引数と対の出口値・射影／ループ不変変数の自由変数捕捉（仮引数に含まれない）／ループの入れ子・ループ内Printの明示エラー／束縛名（関数名・仮引数含む）の一意性。
- evaluator: sum 0..2 ループの正しい実行と対の共有／需要の無いループが走らないこと／反復上限の制御されたエラー／300反復のスタック安全性。
- renderGraph: loopNode・applyNode×2・仮引数ノード（初回反復値）の描画／自由変数捕捉エッジ／ノード数の線形性。
- ブラウザ上での手動UI確認は未実施です（ユーザーによる確認待ち）。特にループのグラフレイアウト（仮引数上段・本体クラスタ・サイクルエッジ）は実表示での調整余地があります。

## §4.6 GHC検証オラクル Phase 1（haskellPrinter.ts）

### 脱糖プリンタ本体（src/compiler/haskellPrinter.ts）
- `printHaskell(program): string` — 関数型ASTからコンパイル可能なdo記法Haskellソースを生成する。本番の実行パスには一切組み込まないCI/開発専用モジュール。
- 脱糖規則（launchbury.mdの対応表・CLAUDE.md §4.2チェックリスト(d)）: Let→do記法内のlet文／LetRec→letの関数束縛（Haskellのletはletrecなので自己参照がそのまま成立）／LetIn→式レベルlet-in／If→if-then-else／Pair→タプル・Proj→fst/snd／Do→main = do { putStrLn (showJS e); ... }／未束縛Var→`undefined`（Haskellの⊥そのもの。§3.6の意味論と正確に一致し、型検査を通過してforce時のみ実行時例外）。
- 識別子マングリング: ユーザー変数は `v_` 接頭辞＋不正文字の単射エスケープ（例: 数→_u6570_）、合成束縛 %cond/%loop/%r は `phi_` 接頭辞。接頭辞で名前空間が分離されるため衝突しない。
- 出力の正規化: 生成ソースに `showJS`（整数値Doubleは"3"、真偽値は小文字、Infinity/NaN）と `jsMod`（JSの%＝被除数の符号・truncated division、除数0はNaN）を埋め込み、evaluate()のconsoleOutputと文字単位で一致させる。数値リテラルは `(n :: Double)` に固定してdefaultingの揺れを防止。
- 既知の境界（モジュールヘッダに明記）: エラーを含む実行は差分テスト対象外／文脈から型が定まらない裸の⊥はGHCが曖昧型エラーで拒否／極端な数値（1e21以上等）は表示形式がJSと異なる。

### 検証
- ゴールデンテスト6件（haskellPrinter.test.ts、GHC不要）: Let/PrimApp/Doの脱糖形、⊥→undefined、if-elseφ、%condマングリング、whileのletrec＋タプル出口＋射影、非ASCII変数名のエスケープ。
- ローカルGHC 9.14.1（brew）による実機確認: 代表5プログラム（算術/IEEE754/jsMod/文字列等価、短絡⊥（§7-5）、if-elseφ、sumループ、if内while）すべてで `ghc -fno-code` 型検査OK・`runghc` 出力がevaluate()と完全一致。短絡プログラムではHaskellの&&がundefinedを触らず"false"を返し、意味論対応がGHC側でも成立することを確認。
- 残作業: Phase 2＝差分テストハーネスの自動化（`npm run test:ghc`。GHC不在環境ではスキップして通常のnpm run testを壊さない構成にする）、Phase 3＝GitHub Actions（haskell-actions/setup + setup-node）への組み込み。→ いずれも下記の通り実装済み。

## §4.6 GHC検証オラクル Phase 2/3（差分テストハーネスとCI）

### Phase 2: 差分テストハーネス（src/compiler/ghcOracle.test.ts / `npm run test:ghc`）
- 8つのfixture（手続型AST）それぞれに「①`ghc -fno-code` による独立型検査 → ②`runghc` 実行 → evaluate() の consoleOutput と標準出力の文字単位一致（観測等価、launchbury.md §3）」を検証する。
- fixture: 算術/IEEE754の0除算/jsModの符号規約/文字列等価/累乗、論理演算（Not/Or）と文字列出力、短絡⊥（検証項目§7-5）、SSA複合代入とメモ化を跨ぐ複数print、if-elseφ＋%cond共有、sumループ（letrec・対・射影）、ループ不変変数の自由変数捕捉、if分岐内while＋需要駆動。
- ゲート: 環境変数 `GHC_ORACLE=1` のときのみ実行（package.jsonの `test:ghc` スクリプトが設定）。通常の `npm run test` ではスイートごとスキップされ、GHC不在環境を壊さない。GHC_ORACLE=1 でも ghc/runghc が見つからない場合は警告してスキップ。
- ローカルGHC 9.14.1 で8fixture全て型検査OK・出力完全一致を確認済み（実行時間 約10秒）。

### Phase 3: GitHub Actions（.github/workflows/ci.yml）
- push（main）/ PR 時に ubuntu-latest 上で `npm ci → build → lint → test → test:ghc` を実行する。GHCは haskell-actions/setup@v2（ghc-version 9.6）で導入。生成コードは base のみに依存するため、ローカル（9.14）とCIのGHCバージョン差は結果に影響しない。
- これにより CLAUDE.md §4.6 の「GHCはCI専用検証オラクル・本番の実行系には一切組み込まない」という位置づけが構造的に完成した。ワークフローは初回push後にGitHub上で有効になる。

## §4.8 手続型参照インタプリタと左ペイン実行結果表示

### 手続型参照インタプリタ（src/compiler/procInterpreter.ts）
- `runProcedural(ast, maxSteps=10000): { output, status: 'ok'|'timeout'|'error', message? }` — 手続型ASTを変換を経由せず正格・逐次・可変な変数表で直接実行する。評価器とコードを共有しない独立実装（参照オラクル）。
- 意味論: And/Orは手続型言語の慣習どおり短絡評価（JS準拠）、Div/ModはIEEE754、未束縛変数の参照は即時エラーで実行停止（関数型側の⊥許容 §3.6 と対照的）。入れ子ループ・ループ内Printにも制限なし（変換側の現行スコープ制限とは独立）。
- 無限ループ対策: ループ反復総数の上限（PROC_MAX_STEPS=10000）超過で「打ち切り」ステータスを返し、**途中までの出力を保持**する。
- 理論上の位置づけ: CLAUDE.md §4.7テンプレートの「①変換元の操作的意味論」の実行可能な定義。3者比較（runProcedural = evaluate∘transpile = runghc）で②変換の正しさまで機械検証が閉じる。

### コンソールの共通フレームと個別リサイズ（ConsolePanel.tsx）
- 左右のコンソールを共通コンポーネント `ConsolePanel`（title＋children）に統一し、見た目の同一性をコンポーネントレベルで保証した。
- 各コンソールの上端に仕切りバー（row-resizeカーソル、中央にグリップ表示）を設け、ドラッグで左右それぞれ**独立に任意の高さ**へリサイズできる（最小40px〜画面高の8割。既定160px）。
- Blockly側はコンソールのリサイズでワークスペース領域が変わるため、ResizeObserver → `Blockly.svgResize()` で描画領域を追従させる（Blocklyはwindowのresizeしか監視しない）。React Flow側は自前のResizeObserverを持つため対応不要。

### 左ペイン下部の「実行結果（手続型）」コンソール（BlocklyPane.tsx）
- 「関数型に変換して実行」押下時、変換とは独立に毎回 runProcedural を実行し、左ペイン下部のコンソールに出力する。見た目は右ペインの仮想コンソール（App.tsx）と完全に同一（#1e1e1e・monospace・`> ` プレフィックス・同じ見出しスタイル）で、見出しを「実行結果（手続型）」／右を「実行結果（関数型）」として区別する。1回の実行で2つのコンソールに結果が並ぶ。
- 打ち切り/エラー時は `> [打ち切り] ...` / `> [エラー] ...` の行として出力される（右コンソールの `[エラー]` 行と同じ流儀）。意図的な無限ループでは「手続型＝打ち切り／関数型＝需要が無ければ停止」という停止性の非対称が左右に並んで見える。
- 変換が現行スコープ外（ループ内Print等）でエラーになる場合でも、手続型側の結果は常に表示される。

### テスト（procInterpreter.test.ts、6件）
- 停止するプログラムでの関数型パイプラインとの観測等価／手続型の短絡評価／無限ループの打ち切り（途中出力の保持）／停止性の非対称（手続型=timeout・関数型=停止）の機械的確認／未束縛変数の即時エラー停止／入れ子ループの実行。

## 開発用コンソール出力のコンパクト整形（debugPrint.ts）
- 「実行」時に console.log していたAST・評価トレースの生オブジェクト（DevToolsのCopy objectで全要素改行される冗長なJSON）を、`src/compiler/debugPrint.ts` の整形文字列に置き換えました。実行パイプラインの一部ではなくログ専用です。
- 整形規則: プログラム構造の節目だけ改行し、式・文の中身は1行に畳む。
  - ①手続型AST: 1文=1行。If/Whileのみ cond/then/else/body をフィールド単位で改行（分岐・本体内の文は1行）。
  - ②関数型AST: Let/LetRecの連鎖に沿って body/fnBody だけを改行・インデント。束縛の右辺式・Doは1行。fnBody直下のIf（ループの骨格）のみ cond/then/else を改行。
  - ③評価トレース: 1イベント=1行（force順に上から読める）。関数値のenv（Map）は {} と表示、Thunkのクロージャは省略。
- コンソール出力（④）は従来どおり配列のまま別行でログします。


