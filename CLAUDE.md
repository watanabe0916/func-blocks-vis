## 0. AI Action Guidelines (CRITICAL RULES)

あなたは本プロジェクトの専属AIエンジニアとして、以下を絶対のルールとして遵守すること。

**Language Policy:**
- Thought Process: All internal reasoning, step-by-step planning, and tool-use scratchpads MUST be in English for maximum logical accuracy.
- User Communication: All conversational outputs, explanations, and markdown directed to the user MUST be in Japanese.

**Orchestration & Planning Model (NEW):**
- 多段の計画立案・タスク分解・アーキテクチャ判断（オーケストレーション/プランニング）には **Claude Fable 5 (Fable)** を用いること。
- 実装フェーズ（後述の外科的編集）は通常モデルで実行してよい。計画→実装の境界を明示し、計画成果物（変更リスト・影響範囲）を先に確定してから編集に入る。

**Surgical Code Editing:**
- コード更新は外科的編集とする。タスク達成に必要な行・関数・ブロックのみを変更する。
- 明示要求または不可避な場合を除き、ファイル全体の上書き・書き直しを禁ずる。
- 既存の import / 状態管理ロジック / UIレイアウト / 未変更機能を厳密に保存する。

**Token Efficiency (NEW):**
- 全文再出力を避け、**差分（対象関数・対象行のみ）** を提示する。
- 型は `src/compiler/types.ts` / `src/compiler/functionalAst.ts` を参照し、既知の型定義を本文に再掲しない。
- 計画は箇条書きで簡潔に。冗長な説明・重複した前置きを排除する。

---

## 1. プロジェクト概要

「手続型プログラミングのブロック（Blockly）」で組み立てたプログラムを、明示的な **関数型AST** を経由して純粋関数として実行し、その評価過程を「純粋関数型のデータフローグラフ（React Flow）」として可視化するWebブラウザ向け学習環境の構築。`eval()` 等によるテキストコードの直接実行は行わない。

実行パイプラインは次の5段階に明確に分離する（詳細は §4）：
**①手続型AST抽出 → ②関数型ASTへの変換 → ③（関数型ASTにのみ依存する）純粋評価器による実行 → ④結果表示 → ⑤評価トレースに基づくデータフローグラフ描画。**
グラフ構築ロジックと評価ロジックを同一処理内で融合させてはならない。

---

## 2. 厳守すべき確定された前提条件（Final）

以下は本研究の核であり、いかなる場合も逸脱するコード（例: Blockly標準ジェネレータによる破壊的代入コードの生成、グラフ構築と評価の再融合）を書いてはならない。

- **対象:** プログラミング初学者、および手続型言語の経験者。
- **目的:** 手続型のメンタルモデル（順次処理・代入）を入り口としつつ、裏側で純粋関数型として実行・可視化し、関数型の利点（高階関数、遅延評価、参照透過なモジュール化）を直感的に体験させ、学習を強く動機づける。
- **UI基盤:** Blockly。ユーザーは手続型パラダイムでブロックを組む。ここに「関数化」「遅延評価リスト」等の特化ブロックを混在させ、関数型概念に自然に触れさせる。
- **変換ターゲット:** 独立した関数型AST（§4.2）。const束縛、第一級関数適用、三項演算子、再帰へアルゴリズム的に変換する。
- **評価戦略（§3 の核）:** 変数の再代入は「新しい環境（スコープ）の生成とシャドウイング」＝SSA として処理する。**順次処理は入れ子の let 束縛の連鎖（direct style）** として表現し、CPS はデフォルトとして採用しない（非局所制御が必要になる将来段階まで導入を遅延する）。**遅延評価は例外的なラップ処理ではなく、システム全体のデフォルト評価規則（call-by-need）として保証する。**
- **実行パイプライン（§4 の核）:** 「①手続型AST抽出 → ②関数型ASTへの変換 → ③純粋評価器による実行 → ④結果表示 → ⑤評価トレースに基づくグラフ描画」の5段階に分離する。評価器（§4.3）はUIに一切依存しない純粋関数とし、可視化（§4.5）は評価器が出力する**トレース**のみを入力とする純粋関数とする。
- **GHCの位置づけ:** GHCは本番の実行系には一切組み込まない。CI/開発時の型検査・差分テストのための**検証オラクル専用**とする（§4.6）。
- **HCI構成:** 左ペインBlockly（入力）、右ペイン上部React Flow（可視化）、右ペイン下部に仮想コンソール（実行結果テキスト出力）。
- **チューリング完全性:** 「最小限の手続型ブロックセット」＋「関数型ASTへの変換器」の組が、論理破綻なく全計算を表現可能であることを構成的に証明する（§5.5 に必要条件）。

---

## 3. 【CORE】評価モデル: Call-by-Need (真の遅延評価)

本節が定める意味論は、§4.3 の純粋評価器 `evaluator.ts` が実装する内部規則である。参照実装の理論的基礎は Launchbury の自然意味論（"A Natural Semantics for Lazy Evaluation", 1993）および Wadsworth のグラフ簡約（共有付き）に置く。

### 3.1 全値は Thunk（遅延セル）である
式の評価結果は「値」ではなく **Thunk** として生成される。Thunk は次の2状態を持つ有限状態機械である。
- `unevaluated`: 未評価。計算を再現するクロージャ（`() => Value`）を保持する。
- `evaluated`: 評価済み。強制（force）済みの結果値をメモ化保持する。

### 3.2 強制は WHNF まで（full NF ではない）
Thunk の強制（force）は **WHNF（弱冠頭正規形 / Weak Head Normal Form）** までしか評価しない。すなわちコンストラクタ（`cons` セル等）の外側だけを暴き、内部の引数 Thunk は未評価のまま残す。
- この「頭だけ暴く」性質が **無限リスト（余再帰, §5.3）** を有限計算内で扱える理論的根拠である。
- リストの tail は常に Thunk として保持され、`take` 等の需要が届いた分だけ順次 force される。

### 3.3 メモ化（call-by-need = call-by-name + sharing）
Thunk は一度 force されたら結果をキャッシュし、以降の要求では再計算せず共有値を返す。
- 単なるサンク（毎回再計算 = call-by-name）では**不可**。同一 Thunk への複数需要で計算量が指数爆発しうるため、**メモ化（sharing）は必須要件**とする。
- 実装は可変セル（クロージャ内の memo フラグ）で行うが、これは **観測的に純粋（benign effect）** である。外部から見た `force()` は同じ入力に同じ結果を返す純関数であり、メモ化の可変性は観測不能なため「純粋関数型」の前提を破らない。この根拠をコードコメントに明記すること。

### 3.4 需要駆動（Demand-Driven）と評価の起点
遅延評価では「何が評価を始動するか（強制点）」が定まらねばならない。本システムでは **仮想コンソールへの出力（I/O アキュムレータ、関数型AST上の `Do` ノード）を需要の根（root of the demand spine）** と定める。
- 「印字する必要がある」という需要が計算グラフの根から末端へ伝播し、必要な Thunk のみが force される。
- 結果として、**需要が届かなかった Thunk は永遠に未評価のまま残る**。この事実は評価器が出力する**トレース**（§4.3）にそのまま記録され、HCI要件「遅延評価による計算のスキップ」を可視化上そのまま実現する（§4.5）。副作用（I/O）を純粋なデータ処理の需要駆動として隔離する設計とも一致する。

### 3.5 短絡評価は「特殊構文」ではなく創発する
`and`, `or` は**特殊構文ではなく通常の関数**として定義する。例: `and = (x, y) => x ? y : false`。
- 全引数はもともと Thunk として渡されるため、`x` が `false` のとき `y` は need（需要）が届かず force されない。**短絡評価は遅延評価の自然な帰結として創発**し、専用の特殊簡約規則を評価器に持たせる必要はない。
- したがって `And`/`Or` は関数型AST上の `PrimApp`（第一級関数適用, §4.2）として一貫して表現でき、「特殊構文を避ける」という設計目標と完全に両立する。

### 3.6 Bottom (⊥) と非停止
遅延評価では非停止計算 ⊥ は全型に属する値である。未評価 Thunk が ⊥ を包んでいても、force されない限りプログラム全体は停止しうる。これが「存在しない変数を含む右辺を持つ `false && (y>0)`」が安全に `false` へ簡約される理論的裏付けである（検証項目 §7-5）。

---

## 4. システムアーキテクチャ：実行パイプラインとモジュール構成（Final）

本節は §1/§2 で確定した5段階パイプラインの、モジュール単位での実装仕様を定める。**設計議論の結論**として、旧版（手続型AST→グラフ構築と評価が融合した単一処理→React Flow、という1段階アーキテクチャ）から本節の構成へ全面移行する。理由は以下の3点：(1) 独立したテキスト形式の関数型プログラム実体が存在しないと、§3のcall-by-need意味論に対する構成的証明の主張が弱くなる、(2) グラフ構築と評価が同一処理に融合していると、§5.2（一般再帰）・§5.4（高階関数）実装時に再利用可能な関数定義を表現できない、(3) 評価器を独立させることで、Launchburyの規則との対応関係論証（§4.7）が可能になる。

### 4.1 パイプライン全体像
```
Blockly ワークスペース
  → ① 手続型AST抽出         extractor.ts          （既存・変更なし）
  → ② 関数型ASTへの変換      transpiler.ts          （役割を変換のみに縮小）
  → ③ 純粋評価器による実行    evaluator.ts          （新設）→ { value, trace }
  → ④ 結果表示               trace中のPrintイベント → 仮想コンソール
  → ⑤ トレース駆動グラフ描画  FlowPane.tsx / store.ts（(functionalAST, trace) の純粋関数）
```
④と⑤は③の出力（`trace`）を共有する兄弟ステージであり、互いに依存しない。②〜⑤の各ステージは他ステージの内部実装に依存してはならず、モジュール境界とステージ境界を一致させる。

### 4.2 関数型AST定義 `src/compiler/functionalAst.ts`（新設）
React Flow・Zustandに一切依存しない、単独でシリアライズ可能な型として定義する。Launchburyの計算断片（ユーザー定義ラムダなし、固定の組込演算子のみ、letrec自己参照なし＝§5.2/§5.4 実装前の現行スコープ）に対応する最小限のノード種別とする：
- `Lit`: リテラル値（`number | string | boolean`）
- `Var`: 変数参照
- `Let`: SSA代入に対応する束縛（`let x_n = e in ...` というネストした direct style。§2の評価戦略と一致）
- `PrimApp`: 固定の組込演算子（算術・比較・論理）への適用
- `Do`: `Print` 文の列（IOアクションの逐次実行に対応する需要の根）

§5.2（一般再帰）・§5.4（高階関数）を実装する際は、`LetRec` / `Lambda` / `Apply`（ユーザー関数）をノード種別として追加する。追加のたびに、(a) extractor.ts のマッピング規則、(b) evaluator.ts の評価規則、(c) FlowPane.tsx の描画規則（§4.5 の apply ノード／部分グラフ視覚規則に従う）、(d) haskellPrinter.ts の脱糖規則（§4.6）、(e) §4.7 の対応関係論証、を機械的に追加する。

### 4.3 純粋評価器 `src/compiler/evaluator.ts`（新設）
- `evaluate(ast: FunctionalAST): { value: Value, trace: TraceEvent[] }` という **UIに一切依存しない純粋関数** として実装する。
- 内部意味論（Thunkの2状態FSM・WHNF強制・メモ化・需要駆動・短絡評価の創発・⊥の健全な扱い）は §3 の規定をそのまま実装する。
- 評価中に発生する各forceイベントを `TraceEvent`（対象ノード識別子・force順序・結果値・メモ化ヒットか否か）として記録し、返り値の `trace` に蓄積する。**トレースが、可視化ステージ（§4.5）が必要とする唯一の情報源** となる。ノードのdata変異など、UI側の状態を直接操作してはならない。

### 4.4 トランスパイラ `src/compiler/transpiler.ts`（役割を縮小）
- 役割を「①手続型AST → ②関数型ASTへの変換」のみに縮小する。評価ロジック（`force`/`BUILTINS` 等）は一切持たず `evaluator.ts` へ委譲する。
- SSAバージョニング（変数再代入時の新環境生成）は本ステージの責務のまま維持する（`Let` ノードの生成規則として表現する）。

### 4.5 トレース駆動グラフ描画 `src/components/FlowPane.tsx` / `src/store.ts`
視覚語彙は Weck & Tichy, "Visualizing Data-Flows in Functional Programs" のデータフロー図設計原則を採用する（抽出・採否の全根拠・原論文の図解は `DateFlow.md` を参照。同論文の変換経路＝λ計算→圏論表現→unificationは、本システムの「評価器トレースから直接グラフを導出する」設計（§4.5冒頭）と競合するため不採用。ノード種別・エッジ・型注釈・部分グラフ等の**視覚表現層のみ**を流用する）。

- `renderGraph(ast: FunctionalAST, trace: TraceEvent[]): { nodes, edges }` という **評価を一切行わない純粋関数** としてグラフ導出ロジックを実装する。
- **ノード種別ごとに外観を区別する**：関数/演算（矩形、`PrimApp`に対応）、値・変数・リテラル（丸／source、`Lit`/`Var`に対応、入力ポート不要）、動的な関数適用（小さな四角の apply ノード。§5.4 実装時、静的定義を持つ関数ノードと明確に視覚的に区別する）、グラフ全体の入口/出口（黒矢印。sink側を `Do`＝需要の根（§3.4）に対応づける）。
- **全エッジに型ラベル（Number/String/Boolean）を付与する**。接続される2ポートは同一型でなければならず、これは既存の型不一致バリデーション（`logic_compare_ext` の `onchange` フック）の理論的裏付けとなる。中間データフローの型をすべて明示することで、コード上見えない型の流れを学習者に一望させる。
- レイアウトは**上から下**への流れに統一する（コード上パラメータが各所に散在するのに比べ、データの生成順を自然に追える読み順を与える）。
- λ式/クロージャ（§5.4実装時）は破線枠の**部分グラフ**として描き、外側グラフから内側部分グラフへの自由変数捕捉エッジで closure を表現する。
- ノードは評価状態を視覚化する：`unevaluated`＝ゴースト/減光、`evaluated`＝強調（メモ化値を表示）。この判定は trace の読み取りにより行い、評価処理中のライブなノード変異には依存しない。
- need が届かず未評価のまま残ったノード（短絡された右辺・未消費の無限リスト tail 等）は減光のまま留まり「計算がスキップされた」ことを直接可視化する。演算結果 `true` はエメラルドグリーン、`false` はソフト赤/グレー。折りたたみ状態 `foldedNodeIds` を Zustand で管理する。
- Elision（折りたたみ表示）＝「trace上でforce済み（＝evaluated）かつ、入力がリテラルのみ／埋め込みリテラルを持つノード」として判定する。未評価ノードは折りたたまず、常に展開されたゴースト状態で表示する。純粋な接続点にすぎない補助ノードは、意味（origin→targetのデータフロー）を保ったまま直結して畳んでよい（簡約の発想の流用。unificationアルゴリズムそのものは導入しない）。
- **健全性指標**：ノード数・エッジ数はブロック数に対しおおむね線形に収まることを目安とする。急激な超線形増加が見られた場合はノード生成規則の設計を疑う。

**本システムの位置づけ（新規性の所在）**: Weck & Tichyの研究はデータフロー構造の静的可視化（高レベルのプログラム理解）を目的とし遅延評価そのものの可視化は範囲外とする一方、ghc-vis等の関連研究は遅延評価と共有（sharing）の可視化に特化する。本システムはこの両者の中間に位置し、前者の視覚語彙（ノード/エッジ/型/部分グラフ）を骨格に、後者の関心事（force済み/未評価ゴースト状態、§3.1〜3.4）を状態レイヤとして重ねる統合を行う。

### 4.6 GHC＝CI専用検証オラクル `src/compiler/haskellPrinter.ts`（新設）
- GHC（`ghc`/`runghc`）は **本番の実行パスに一切組み込まない**。理由：(a) GHC起動・コンパイルのレイテンシがBlockly「実行」ボタンの即時フィードバックというHCI要件を損なう、(b) 生成コードをサーバー側で実行するにはサンドボックス化が必須で安定運用上のリスクが増す、(c) 最も本質的な理由として、GHCの標準出力だけでは評価トレース（§4.3 の `TraceEvent`＝本研究の可視化対象そのもの）を取得できない。トレース取得には Hat/ghc-debug 級の独立した計装が必要であり、これは現行スコープとは別規模の研究課題であるため見送る。
- `haskellPrinter.ts` は関数型AST（§4.2）から、実際にコンパイル可能なdo記法Haskellソースを生成する。CI/開発時のみ、以下2段階の検証に用いる：
  1. **型検査**：`ghc -fno-code` により構文・型を独立検証する（コード生成・リンクを省略した軽量チェック）。`variables_get` の型抜け穴等、Blockly側の手書きバリデーション（`logic_compare_ext` の `onchange` フック）では検出できない誤りを、GHCの型システムで機械的に検出する。
  2. **差分テスト**：`runghc` により実際に実行し、標準出力を `evaluate()` の結果（trace中のPrintイベント列）と突き合わせる。最終値の意味論的な独立検証とする。
- 学習者向けUIには、Haskell風プリティプリント結果を **デフォルト非表示・任意展開の補助表示** として提供してよい（初学者の負担を増やさないため）。

### 4.7 Launchbury対応関係論証の要件
- `evaluator.ts` の実装時、以下の対応表をコードコメントまたは別ドキュメントとして記述する：`Lit`↔LIT規則、`Var`↔VAR規則（ヒープ更新=メモ化）、`Let`↔LET規則↔Haskellの `do{let decls;stmts}=let decls in do{stmts}`、`PrimApp`↔PRIMOP拡張規則、`Do`(Printの列)↔トップレベル評価の起点↔Haskellの `do{e;stmts}=e >> do{stmts}`。
- この論証が低コストで成立するのは、ユーザー定義ラムダ・自己参照letrecが存在しない現行スコープに限られる。§5.2（一般再帰）・§5.4（高階関数）の実装に着手する **前に** 、この対応関係論証を一度確定しておくこと（実装後は計算断片が拡大し証明コストが非連続的に増大するため）。

---

## 5. 今後の設計との整合（前方互換 / 実装前に確定）

以下は次段階以降の設計指針であり、本版の call-by-need 基盤（§3）およびパイプラインアーキテクチャ（§4）の上で一貫して成立する。

### 5.1 条件分岐 If-Else
- 三項演算子 `cond ? a : b` へ変換する。評価器は **scrutinee（cond）のみ WHNF まで force** し、選ばれなかった分岐 Thunk は **force しない**（遅延の自然な帰結）。
- SSA の合流点で必要な **φ関数は、三項演算子の返り値そのものが担う**ため専用ノード型は不要（Appel "SSA is Functional Programming" の対応）。
- 関数型AST（§4.2）上は既存の `PrimApp` の枠組み、または専用の `If` ノードとして追加する。

### 5.2 再帰（一般再帰）
- `letrec`（自己参照束縛）で表現する。**Thunk は定義時に force されない**ため、束縛名が自分自身を参照しても定義時に無限展開せず、自己参照が健全に成立する（不動点）。
- 停止条件を実行時値で判定する分岐（§5.1）と組み合わせ、`while` 相当の**非有界計算 = 一般再帰**を得る。
- 関数型AST（§4.2）上は `LetRec` ノードとして追加する。追加時は §4.7 の対応関係論証を先に更新すること（自己参照のブラックホール検出・⊥の扱いはLaunchbury原論文でも比較的デリケートな部分であり、実装難度が非連続的に上がる境界である）。

### 5.3 余再帰（Corecursion）/ 無限リスト
- 無限リスト生成規則（例 `iterate(f,x0)=cons(x0, iterate(f,f(x0)))`）は §5.2 と**同じ自己参照束縛ブロックを流用**して実装してよい。
- ただしこれは停止する再帰（帰納）ではなく **余再帰（余帰納）** である。WHNF（§3.2）+ tail Thunk + `take` による強制有限化がこの区別を実務上吸収する。**内部ドキュメント/コメントには「recursion ではなく corecursion」と明記**し、チューリング完全性（停止性）の議論と混同しないこと。

### 5.4 高階関数
- ラムダ値（関数を第一級の値とするブロック）、`Apply`、`map`/`filter`/`reduce`、`compose`、部分適用を導入する。引数は本基盤ではすべて Thunk として渡るため、有限リスト用の `map` 等が**無限リストにもそのまま適用可能**（抽象の再利用性 = モジュール性）となる。
- 関数型AST（§4.2）上は `Lambda` / `Apply`（ユーザー関数）ノードとして追加する。この時点で評価器は固定BUILTINSへのPRIMOP適用だけでなく、環境拡張を伴う汎用APP規則（引数の束縛と本体評価）が必要になる（§4.7 参照）。
- 可視化は §4.5 の規則に従う：λ式/クロージャは破線枠の部分グラフとして描き、静的定義を持つ組込演算子ノード（矩形）と動的な関数値の適用（小四角の apply ノード）を視覚的に区別する。

### 5.5 チューリング完全性の必要条件（死守）
- 計算能力を保証するのは **「一般再帰（自己参照 letrec, §5.2）」＋「実行時値に依存する条件分岐（§5.1）」** のみである。
- **map/filter/reduce 等の高階関数、および遅延評価それ自体は計算能力を追加しない**（有限リストへの構造的再帰は原始再帰的にとどまる）。これらは「モジュール性・利便性・教育効果」のための層であり、チューリング完全性の証明には寄与しない。有界 for のみでは原始再帰関数までしか表現できない（Meyer & Ritchie: loop 言語 vs while 言語）点に注意。

---

## 6. 実施済みタスクの記録（アーカイブ）

以下は本ドキュメントの過去バージョンの指示に基づき実施され、完了・検証済みのタスクである。詳細な実施内容・変更履歴は `memo.txt`（§8の規約に基づく変更履歴）を参照する。**今後の実装作業への直接の指示としては参照しないこと**（最新のアーキテクチャ指示は §1〜§5 を参照すること）。

- 論理・比較演算子ブロック（`logic_compare_ext` / `logic_operation_ext` / `logic_negate_ext` / `logic_boolean_ext`）および算術演算子ブロックの call-by-need 意味論への全面刷新（Thunk化・And/Or特殊分岐の撤去・Elision再定義・0除算のIEEE754準拠化・textブロック復元等）。この時点ではグラフ構築と評価が `transpiler.ts` に融合した1段階アーキテクチャであったが、§4 の全面移行によりこの構成は置き換えられる。

---

## 7. Verification Plan

**Automated:**
- `npm run build`（型チェック合格）、`npm run lint`（規約違反なし）。
- CI: `haskellPrinter.ts` が生成したHaskellソースに対する `ghc -fno-code` 型検査、および `runghc` による差分テスト（§4.6）。本番ビルド・実行系には含めない。

**Manual:** Blockly で以下を構成し「関数型に変換して実行」を押下。
1. **リテラル比較の Elision:** `3 < 5` → 単一値ノード `true` として折りたたみ表示。展開で `3<5` 詳細確認可。
2. **変数の部分適用表現:** `x=3`, `x<5` → 変数ノード `x_1` から比較演算ノード `<5`（引数内部埋め込み）へ単一エッジ接続。
3. **論理演算の簡約:** `true AND false` → 単一値ノード `false`。
4. **UI 堅牢性:** 演算ノードの詳細/簡易切替でエッジ再計算（レイアウト）が破綻しない。
5. **短絡＝遅延の創発確認:** `false AND (未定義変数 y > 0)` 実行時、右辺 Thunk に **need が届かず force されない**ことを確認する。右辺ノードは**未評価（ゴースト）のまま**留まり、全体が `false` に簡約され、未定義変数エラーでクラッシュしないこと。
6. **遅延と WHNF/メモ化:** 無限リスト（`iterate` 相当）から `take 3` を実行し、(a) 全体が停止すること、(b) 消費されていない tail ノードが未評価のまま残ること、(c) 同一 Thunk が再計算されず1回だけ評価される（メモ化）ことを確認。
7. **【追加】パイプライン分離の健全性確認:** `evaluate()` が返す `trace` のみから `renderGraph()` がグラフを再構築できること（評価処理中のライブなノード変異に一切依存していないこと）をユニットテストで確認する。`transpiler.ts`（②）・`evaluator.ts`（③）・`FlowPane.tsx`（⑤）がそれぞれ単体でテスト可能であることを確認する。
8. **【追加】視覚設計原則の適用確認（§4.5 / `DateFlow.md`）:** 全エッジに型ラベル（Number/String/Boolean）が表示されること、レイアウトが上→下の流れで描画されること、ノード種別（関数/演算＝矩形、値・変数＝丸、start/sink＝黒矢印）が視覚的に区別されること。

---

## 8. ブロック機能のドキュメント化規約（memo.txt）

1. 新しいカスタムブロック/評価規則/データフロー可視化機能を追加した際は、必ずプロジェクトルートの `memo.txt` に機能内容を簡潔に追記する。
2. 既存機能を変更・修正した場合は `memo.txt` の該当記述を現状に合わせて修正する。
3. このドキュメント化プロセスは今後の機能追加・仕様変更でも継続的に行う。
4. 追記・修正は、**実装（コード変更）および検証（ビルド/リンター合格、CI差分テスト合格）が完全に完了し正常動作を確認したフェーズの最後**に行う。実装途中・検証前の追記は厳禁とする。