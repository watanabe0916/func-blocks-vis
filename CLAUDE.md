## 0. AI Action Guidelines (CRITICAL RULES)

あなたは本プロジェクトの専属AIエンジニアとして、以下を絶対のルールとして遵守すること。

**Language Policy:**
- Thought Process: All internal reasoning, step-by-step planning, and tool-use scratchpads MUST be in English for maximum logical accuracy.
- User Communication: All conversational outputs, explanations, and markdown directed to the user MUST be in Japanese.

**Orchestration & Planning Model (NEW):**
- 多段の計画立案・タスク分解・アーキテクチャ判断（オーケストレーション/プランニング）には **Claude Fable 5 (Fable)** を用いること。
- 実装フェーズ（後述の外科的編集）は通常モデルで実行してよい。計画→実装の境界を明示し、計画成果物（変更リスト・影響範囲）を先に確定してから編集に入る。

**Surgical Code Editing:**
- コード更新は外科的編集とする。タスク達成に必要な行・関数・ブロックのみを変更する。
- 明示要求または不可避な場合を除き、ファイル全体の上書き・書き直しを禁ずる。
- 既存の import / 状態管理ロジック / UIレイアウト / 未変更機能を厳密に保存する。

**Token Efficiency (NEW):**
- 全文再出力を避け、**差分（対象関数・対象行のみ）** を提示する。
- 型は `src/compiler/types.ts` を参照し、既知の型定義を本文に再掲しない。
- 計画は箇条書きで簡潔に。冗長な説明・重複した前置きを排除する。

---

## 1. プロジェクト概要

「手続型プログラミングのブロック（Blockly）」で組み立てたプログラムを、「純粋関数型のデータフローグラフ（React Flow）」として可視化・実行するWebブラウザ向け学習環境の構築。`eval()` 等によるテキストコードの直接実行は行わず、すべて **AST（抽象構文木）** と **グラフデータ** の変換によって計算を成立させる。

---

## 2. 厳守すべき確定された前提条件（Final）

以下は本研究の核であり、いかなる場合も逸脱するコード（例: Blockly標準ジェネレータによる破壊的代入コードの生成）を書いてはならない。

- **対象:** プログラミング初学者、および手続型言語の経験者。
- **目的:** 手続型のメンタルモデル（順次処理・代入）を入り口としつつ、裏側で純粋関数型TypeScriptとして実行・可視化し、関数型の利点（高階関数、遅延評価、参照透過なモジュール化）を直感的に体験させ、学習を強く動機づける。
- **UI基盤:** Blockly。ユーザーは手続型パラダイムでブロックを組む。ここに「関数化」「遅延評価リスト」等の特化ブロックを混在させ、関数型概念に自然に触れさせる。
- **変換ターゲット:** TypeScript（純粋関数型サブセット）。const束縛、アロー関数、三項演算子、再帰へアルゴリズム的に変換する。
- **評価戦略（本版で確定 / §3 の核）:** 変数の再代入は「新しい環境（スコープ）の生成とシャドウイング」＝SSA として処理する。**順次処理は入れ子の let 束縛の連鎖（direct style）** として表現し、CPS はデフォルトとして採用しない（非局所制御が必要になる将来段階まで導入を遅延する）。**遅延評価は例外的なラップ処理ではなく、システム全体のデフォルト評価規則（call-by-need）として保証する。**
- **HCI構成:** 左ペインBlockly（入力）、右ペイン上部React Flow（可視化）、右ペイン下部に仮想コンソール（実行結果テキスト出力）。
- **チューリング完全性:** 「最小限の手続型ブロックセット」＋「関数型TSへの変換器」の組が、論理破綻なく全計算を表現可能であることを構成的に証明する（§5.5 に必要条件）。

---

## 3. 【CORE】評価モデル: Call-by-Need (真の遅延評価)

本システムの全評価器・全データ構造は、以下の意味論に従う。参照実装の理論的基礎は Launchbury の自然意味論（"A Natural Semantics for Lazy Evaluation", 1993）および Wadsworth のグラフ簡約（共有付き）に置く。

### 3.1 全値は Thunk（遅延セル）である
式の評価結果は「値」ではなく **Thunk** として生成される。Thunk は次の2状態を持つ有限状態機械である。
- `unevaluated`: 未評価。計算を再現するクロージャ（`() => Value`）を保持する。
- `evaluated`: 評価済み。強制（force）済みの結果値をメモ化保持する。

### 3.2 強制は WHNF まで（full NF ではない）
Thunk の強制（force）は **WHNF（弱冠頭正規形 / Weak Head Normal Form）** までしか評価しない。すなわちコンストラクタ（`cons` セル等）の外側だけを暴き、内部の引数 Thunk は未評価のまま残す。
- この「頭だけ暴く」性質が **無限リスト（余再帰, §5.3）** を有限計算内で扱える理論的根拠である。
- リストの tail は常に Thunk として保持され、`take` 等の需要が届いた分だけ順次 force される。

### 3.3 メモ化（call-by-need = call-by-name + sharing）
Thunk は一度 force されたら結果をキャッシュし、以降の要求では再計算せず共有値を返す。
- 単なるサンク（毎回再計算 = call-by-name）では**不可**。同一 Thunk への複数需要で計算量が指数爆発しうるため、**メモ化（sharing）は必須要件**とする。
- 実装は可変セル（クロージャ内の memo フラグ）で行うが、これは **観測的に純粋（benign effect）** である。外部から見た `force()` は同じ入力に同じ結果を返す純関数であり、メモ化の可変性は観測不能なため「純粋関数型」の前提を破らない。この根拠をコードコメントに明記すること。

### 3.4 需要駆動（Demand-Driven）と評価の起点
遅延評価では「何が評価を始動するか（強制点）」が定まらねばならない。本システムでは **仮想コンソールへの出力（I/O アキュムレータ）を需要の根（root of the demand spine）** と定める。
- 「印字する必要がある」という需要が計算グラフの根から末端へ伝播し、必要な Thunk のみが force される。
- 結果として、**需要が届かなかった Thunk は永遠に未評価のまま残る**。これは HCI 要件「遅延評価による計算のスキップ」を可視化上そのまま実現する（§4.3）。副作用（I/O）を純粋なデータ処理の需要駆動として隔離する設計とも一致する。

### 3.5 短絡評価は「特殊構文」ではなく創発する（旧版矛盾の解消）
旧版は「全演算を第一級関数適用として扱う」一方で「And/Or の右辺だけ特殊に Thunk 化する」と規定しており、これは正格評価前提では非両立だった。**call-by-need を全面採用する本版では、この矛盾は消滅する。**
- `and`, `or` は**特殊構文ではなく通常の関数**として定義できる。例: `and = (x, y) => x ? y : false`。
- 全引数はもともと Thunk として渡されるため、`x` が `false` のとき `y` は need（需要）が届かず force されない。**短絡評価は遅延評価の自然な帰結として創発**し、専用の特殊簡約規則を評価器に持たせる必要はない。
- したがって `And`/`Or` は `ApplyNode`（第一級関数適用, §4）として一貫して表現でき、「特殊構文を避ける」という設計目標と完全に両立する。

### 3.6 Bottom (⊥) と非停止
遅延評価では非停止計算 ⊥ は全型に属する値である。未評価 Thunk が ⊥ を包んでいても、force されない限りプログラム全体は停止しうる。これが「存在しない変数を含む右辺を持つ `false && (y>0)`」が安全に `false` へ簡約される理論的裏付けである（検証項目 §7-5）。

---

## 4. データ構造・可視化への影響

### 4.1 型定義 `src/compiler/types.ts`
- `Thunk` 型を新設する（2状態: unevaluated / evaluated、`nodeId` 参照を保持）。
- 環境は **`Env = Map<VarName, Thunk>`**（値ではなく Thunk を束縛）とする。
- `ApplyNode`（第一級関数適用）を定義し、**引数フィールドは値ではなく Thunk（未評価式）への参照**とする。
- `LiteralNode.value` に `boolean` を追加。`ExpressionNode` に `ApplyNode` を追加。

### 4.2 トランスパイラ `src/compiler/transpiler.ts`
- `ApplyNode` 評価: 関数位置を WHNF まで force し、引数は Thunk のまま束縛して本体へ渡す。
- **And/Or の特殊短絡ロジックは撤去**する。`and`/`or` を通常の組込関数として定義し、短絡は §3.5 の遅延評価から創発させる。
- **Elision（簡約表示）** = 「WHNF まで force 済みかつメモ化された Thunk ノード」。リテラル同士の演算（`3<5`）はノードを消去せず内部保持し、React Flow 初期描画で結果1ノードへ折りたたむ。展開で途中経過を on-demand inspection 可能にする。
- **部分適用構造:** 引数に変数を含む演算（`x<5`）は演算ノードを生成し変数ノードからエッジ接続、リテラル引数はノード内部 `data` に保持する。将来の `filter (<5)` を見据え「引数が1つ空いた関数値」へ昇格可能な構造を維持する。なお `(<5)` は第2引数を固定する**右セクション**であり、素朴なカリー化 `Apply(Apply(<,x),5)` とは引数順が逆になる点に留意（`flip` 相当、またはセクション専用構築規則が必要）。

### 4.3 可視化 `src/components/FlowPane.tsx` / `src/store.ts`
- ノードは **評価状態を視覚化**する: `unevaluated` = ゴースト/減光、`evaluated` = 強調（メモ化値を表示）。
- **需要（demand）と値（value）の双方向アニメーション**: 根（コンソール）から末端への需要伝播と、末端から根への値の戻りを流す。need が届かず未評価のまま残ったノード（短絡された右辺・未消費の無限リスト tail 等）は減光のまま留まり、「計算がスキップされた」ことを直接可視化する。
- 演算結果 `true` はエメラルドグリーン、`false` はソフト赤/グレー。折りたたみ状態 `foldedNodeIds` を Zustand で管理・永続化。

---

## 5. 今後の設計との整合（前方互換 / 実装前に確定）

以下は次段階以降の設計指針であり、本版の call-by-need 基盤の上で一貫して成立する。

### 5.1 条件分岐 If-Else
- 三項演算子 `cond ? a : b` へ変換する。評価器は **scrutinee（cond）のみ WHNF まで force** し、選ばれなかった分岐 Thunk は **force しない**（遅延の自然な帰結）。
- SSA の合流点で必要な **φ関数は、三項演算子の返り値そのものが担う**ため専用ノード型は不要（Appel "SSA is Functional Programming" の対応）。

### 5.2 再帰（一般再帰）
- `letrec`（自己参照束縛）で表現する。**Thunk は定義時に force されない**ため、束縛名が自分自身を参照しても定義時に無限展開せず、自己参照が健全に成立する（不動点）。
- 停止条件を実行時値で判定する分岐（§5.1）と組み合わせ、`while` 相当の**非有界計算 = 一般再帰**を得る。

### 5.3 余再帰（Corecursion）/ 無限リスト
- 無限リスト生成規則（例 `iterate(f,x0)=cons(x0, iterate(f,f(x0)))`）は §5.2 と**同じ自己参照束縛ブロックを流用**して実装してよい。
- ただしこれは停止する再帰（帰納）ではなく **余再帰（余帰納）** である。WHNF（§3.2）+ tail Thunk + `take` による強制有限化がこの区別を実務上吸収する。**内部ドキュメント/コメントには「recursion ではなく corecursion」と明記**し、チューリング完全性（停止性）の議論と混同しないこと。

### 5.4 高階関数
- ラムダ値（関数を第一級の値とするブロック）、`Apply`、`map`/`filter`/`reduce`、`compose`、部分適用を導入する。引数は本基盤ではすべて Thunk として渡るため、有限リスト用の `map` 等が**無限リストにもそのまま適用可能**（抽象の再利用性 = モジュール性）となる。

### 5.5 チューリング完全性の必要条件（死守）
- 計算能力を保証するのは **「一般再帰（自己参照 letrec, §5.2）」＋「実行時値に依存する条件分岐（§5.1）」** のみである。
- **map/filter/reduce 等の高階関数、および遅延評価それ自体は計算能力を追加しない**（有限リストへの構造的再帰は原始再帰的にとどまる）。これらは「モジュール性・利便性・教育効果」のための層であり、チューリング完全性の証明には寄与しない。有界 for のみでは原始再帰関数までしか表現できない（Meyer & Ritchie: loop 言語 vs while 言語）点に注意。

---

## 6. 既存タスクへの移行指示（論理・比較演算子ブロック）

前版の実装計画を call-by-need 基盤へ整合させる。**外科的編集**で以下を反映する。

- `types.ts`: `Thunk` 追加、`Env` を Thunk 束縛へ、`ApplyNode` の引数を Thunk 参照化、`LiteralNode` に boolean 追加。
- `extractor.ts`: `logic_compare_ext` / `logic_operation_ext` / `logic_negate_ext` / `logic_boolean_ext` を、すべて `Apply`（`LessThan`/`GreaterThan`/`Equal`/`NotEqual`/`And`/`Or`/`Not`）へマッピング（変更なし方針を踏襲）。
- `transpiler.ts`: **And/Or 用の特殊短絡分岐を撤去**し組込関数化（短絡は遅延から創発）。Elision = force済みメモ化 Thunk の折りたたみ。部分適用構造を維持。
- `BlocklyPane.tsx`: 論理・比較・真偽値ブロック定義、Boolean 専用コネクタ形状/色、同一型のみ接続許可のバリデーションフック、ツールボックス新カテゴリ「論理・比較」。
- `FlowPane.tsx` / `store.ts`: `opNode`/`valNode`、Elision/詳細のトグル、true/false ハイライト、`foldedNodeIds` 永続化、**評価状態（未評価ゴースト/評価済強調）の描画**。

---

## 7. Verification Plan

**Automated:** `npm run build`（型チェック合格）、`npm run lint`（規約違反なし）。

**Manual:** Blockly で以下を構成し「関数型に変換して実行」を押下。
1. **リテラル比較の Elision:** `3 < 5` → 単一値ノード `true` として折りたたみ表示。展開で `3<5` 詳細確認可。
2. **変数の部分適用表現:** `x=3`, `x<5` → 変数ノード `x_1` から比較演算ノード `<5`（引数内部埋め込み）へ単一エッジ接続。
3. **論理演算の簡約:** `true AND false` → 単一値ノード `false`。
4. **UI 堅牢性:** 演算ノードの詳細/簡易切替でエッジ再計算（レイアウト）が破綻しない。
5. **【重要・改訂】短絡＝遅延の創発確認:** `false AND (未定義変数 y > 0)` 実行時、右辺 Thunk に **need が届かず force されない**ことを確認する。右辺ノードは**未評価（ゴースト）のまま**留まり、全体が `false` に簡約され、未定義変数エラーでクラッシュしないこと。（特殊分岐ではなく遅延評価の帰結として成立していることを、右辺ノードの未評価状態で観測する。）
6. **【追加】遅延と WHNF/メモ化:** 無限リスト（`iterate` 相当）から `take 3` を実行し、(a) 全体が停止すること、(b) 消費されていない tail ノードが未評価のまま残ること、(c) 同一 Thunk が再計算されず1回だけ評価される（メモ化）ことを確認。

---

## 8. ブロック機能のドキュメント化規約（memo.txt）

1. 新しいカスタムブロック/トランスパイラ/データフロー可視化機能を追加した際は、必ずプロジェクトルートの `memo.txt` に機能内容を簡潔に追記する。
2. 既存機能を変更・修正した場合は `memo.txt` の該当記述を現状に合わせて修正する。
3. このドキュメント化プロセスは今後の機能追加・仕様変更でも継続的に行う。
4. 追記・修正は、**実装（コード変更）および検証（ビルド/リンター合格）が完全に完了し正常動作を確認したフェーズの最後**に行う。実装途中・検証前の追記は厳禁とする。